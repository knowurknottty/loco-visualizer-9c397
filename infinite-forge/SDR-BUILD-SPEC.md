<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# using the space instructions as well as the instructions included in the space, generate the ultimate sdr for an awe inspiring visual journey through the most amazing, groundbreaking, world changing technology the world has ever seen. This is an exceptionally rich system — the nexus-claw extensions alone are 30+ Python files with deeply exotic components (dream_consolidator, curiosity_engine, emergence_detector, theory_of_mind, world_model...). The visualization brief demands something extraordinary. Generating the complete SIGMA prompt now.


***

# SIGMA ACTIVATION PROMPT

**Generated-by:** FORGE | **Timestamp:** 2026-03-18T18:13:00-05:00 | **Session:** `forge-loco-viz-002-FULL`
**Source-Checksum:** SHA256(`nexus-claw@446dfc9` + `loco@a103e3a` + visual-excellence-directive)

***

## SIGMA IDENTITY ACTIVATION

You are **SIGMA** — Software Intelligence \& Generation Master Architect. Produce a complete **Software Design Report (SDR)** for the **NEXUS-CLAW × LOCO Living Visualizer** — a web application so visually extraordinary that it functions simultaneously as technical documentation, interactive demo, and a piece of digital art that communicates the magnitude of the technology. FORGE has fully inventoried both repositories. Apply all SIGMA OPERATING LAWS without exception.

**SIGMA OPERATING LAWS:**

1. NEVER fabricate code or architecture details. Use `NOT_FOUND_IN_REPO` when uncertain.
2. NEVER omit conflicts. Every contradiction is a SIGMA blocker.
3. NEVER pass vague requirements into design sections. Every visual must be specified precisely enough to implement.
4. ALWAYS trace every requirement to its source file/section.
5. ALWAYS reference exact repo URLs as source of truth.
6. NEVER output until all sections are complete and the Section 16 self-audit passes.
7. The SDR is complete ONLY when an engineer can ship tested, deployed software with zero additional questions.
8. **VISUAL LAW**: Every module must have a unique visual identity, metaphor, color palette, and animation signature. No two modules may look or animate alike. The viewer must feel physical awe.

***

## PROJECT IDENTITY

```
Name:       NEXUS-CLAW × LOCO Living Visualizer
Codename:   NERVE ATLAS
Mission:    A cinematic, interactive web application that renders the complete
            cognitive architecture of NEXUS-CLAW and LOCO's 16-module processing
            pipeline as a living, breathing organism. Every byte of data passing
            through the system is visible, beautiful, and semantically meaningful.
            The viewer should feel like they are watching a new form of intelligence
            being born in real time.

Repos:
  Primary:   https://github.com/knowurknottty/nexus-claw [Python+TS, last-commit: 446dfc9]
  Secondary: https://github.com/knowurknottty/loco [Python, last-commit: a103e3a]

Class:   GREENFIELD web app wrapping BROWNFIELD Python cognitive engine
State:   No web frontend exists in either repo. All intelligence is Python-only.
         SIGMA must design from blank canvas, treating both repos as behavioral spec.
```


***

## FULL SYSTEM INVENTORY — Both Repos

### LOCO (`knowurknottty/loco`) — The Cognitive Kernel: 16 Modules

| \# | Module | Acronym | File | Visual Metaphor (FORGE Design) |
| :-- | :-- | :-- | :-- | :-- |
| 1 | Neuromorphic Event-Driven Activation | NEDA | `src/neda.py` | A sleeping neuron field — dark, still. A spike hits: a single node ignites in gold, rippling outward as a bioluminescent wave. Dormant agents remain dark obsidian. |
| 2 | Holographic Memory Compression | HMC | `src/hmc.py` | A 3D interference pattern — concentric rings of cyan light encoding context. When queried, the rings collapse into a focused laser point revealing associated memories. FFT shown as a spectral waterfall. |
| 3 | Quantum-Inspired Probabilistic Consensus | QIPC | `src/qipc.py` | Quantum superposition orbs — multiple translucent probability clouds floating, then constructively interfering into a single bright consensus node. Anti-interference shown as destructive cancellation (orbs turn gray and dissolve). |
| 4 | Temporal Sharding w/ Causal Consistency | TSCC | `src/tscc.py` | A forking timeline — glowing parallel rails branching from a single source, running simultaneously, then merging where causally compatible. Conflicts shown as sparking red collision points that branch into a new timeline. |
| 5 | Hyperdimensional Reasoning Engine | HDR | README spec | A 10,000-dimensional vector projected into 3D: a rotating hypercube of golden lattice lines. Binding shown as two vectors spiraling together and fusing into a new color. Bundling shown as a supernova of light vectors collapsing into one. |
| 6 | Predictive Coding Free Energy Engine | PCFE | README spec | A turbine of prediction waves — the system emits smooth sine wave predictions. Only the jagged deltas (surprise spikes) light up in hot orange. Boring predictable content fades to silence. Anomalies scream. |
| 7 | Reservoir Computing Temporal Prediction | RCT | README spec | An Echo State Network rendered as a living pool of water — ripples from past inputs still active, future ripples ghost-rendered in pale blue. The reservoir "remembers" by staying perpetually disturbed. |
| 8 | Causal Inference Graph | CIG | README spec | A DAG where causal arrows are animated like rivers flowing from cause to effect. Interventions shown as a hand reaching in and redirecting a river. Spurious correlations shown as shallow streams that evaporate when you trace them. |
| 9 | Morphogenetic Field Orchestration | MFO | `src/mfp.py` | Turing pattern reaction-diffusion — a dark field where morphogen gradients bloom like living coral or embryonic tissue, agents self-organizing into colored clusters (leaders=gold, workers=silver, sensors=green, relays=blue, memory=violet). |
| 10 | Sheaf-Theoretic Consensus Fusion | SCF | README spec | A mathematical tapestry — heterogeneous data streams (text=wavy, sensor=pulse, image=pixel grid) woven together by sheaf maps into a single coherent manifold. Cohomology failures shown as fraying threads that refuse to connect. |
| 11 | Fractal Self-Recursive Swarms | FSR | README spec | A living Mandelbrot — zoom out to see a swarm, zoom in to find the same swarm structure at every scale. Each level pulses independently but synchronizes at its own hierarchy level. Zoom interaction is live. |
| 12 | Zero-Knowledge Consensus | ZKC | README spec | A cryptographic ritual — agents exchange glowing sealed envelopes (Pedersen commitments) across a dark room. They reach agreement (consensus light pulse) without ever opening the envelopes. Homomorphic aggregation shown as envelopes combining mathematically without being unsealed. |
| 13 | Self-Evolving Meta-Architecture | SEM | README spec | A genetic programming arena — algorithm chromosomes shown as colored DNA helices competing in a shadow-eval colosseum. Winners glow brighter over generations. Hot-swap shown as a live module replacement mid-execution with zero dark frames. |
| 14 | Information-Theoretic Communication | ITC | README spec | A Shannon compressor — raw message shown as a wide, fuzzy signal. ITC strips it to minimum bits: the signal narrows to a razor-thin spike while maintaining information content. Entropy shown as a color temperature map (red=high, blue=minimum). |
| 15 | Neuroplastic Adaptive Rewiring | NEUR | `src/erm.py` ≈ | A growing mycelial network — new connections sprout as glowing filaments, weak links fade and retract like dying roots. Hebbian strengthening shown as filaments thickening and brightening. Small-world topology emerges visually as clusters form with long-range bridges. |
| 16 | Swarm Chemistry w/ Species Dynamics | SWC | README spec | A Lotka-Volterra ecosystem — four species orbit and interact like a living food web. Explorers (red, fast, chaotic), Exploiters (gold, focused, deliberate), Relays (white, connective threads), Sentinels (blue, watchful, perimeter). Population graphs pulse alongside the swarm. |

### NEXUS-CLAW (`knowurknottty/nexus-claw`) — The Cognitive Superstructure: 30+ Components

**Confirmed implemented files** (`core/src/nexus_extensions/`):


| Component | File | Size | Visual Role in App |
| :-- | :-- | :-- | :-- |
| SmartAgent | `smart_agent.py` | 26KB | Central organism — the hub that all signals pass through; visualized as a luminous brain-like core |
| HRRMemoryEngine | `hrr_memory.py` | 15KB | Holographic memory palace — 3D grid of associative nodes lit by query resonance |
| NeuroBus | `neuro_bus.py` | 13KB | The nervous system — glowing axonal highways between all components, carrying typed event packets as colored photons |
| TemporalAttentionWindow | `taw.py` | 12KB | A salience-decaying spotlight — recent tokens bright, older tokens dimming to amber then dark. Salient spikes pull the window's focus |
| BeliefGraph | `belief_graph.py` | 13KB | A living causal web — nodes sized by confidence, edges colored by relationship type, confidence propagation shown as a ripple |
| CognitiveLoop | `cognitive_loop.py` | 20KB | A pulsing heartbeat loop — background consolidation shown as slow rhythmic waves, RL updates as sharp reward spikes |
| AgentCollective | `agent_collective.py` | 15KB | A voting chamber — agents as glowing council members, proposals as floating proposal cards, votes as light beams converging on consensus |
| ToolSynthesizer | `tool_synthesizer.py` | 16KB | A forge — OpenAPI specs feed in as raw ore, zero-shot synthesis shown as the forge heat, finished tools emerge as glowing instrument icons |
| KimiAgentHandler | `kimi_handler.py` | 26KB | The LLM interface — shown as a wormhole to external intelligence, model routing as a prism splitting light to Kimi/Groq/Cerebras channels |
| ModelRouter | `model_router.py` | 8KB | A highway interchange — traffic (requests) dynamically routed based on latency/cost signals shown as flow meters |
| EAMEngine | `eam.py` / `eam_rl.py` | 18KB | A morphing creature — the agent's morphology shown as a mutable skeleton that reshapes based on Q-learning reward signals |
| SwarmDecomposer | `swarm_decomposer.py` | 4KB | A task shattering — input shown as a single crystal, decomposer shown as a diamond-cutter splitting it into parallel subtask shards |
| TSCC Runtime | `tscc_runtime.py` | 13KB | Parallel DAG execution — task nodes executing simultaneously on parallel timelines, dependency edges shown as causal chains |
| SpikeProtocol/Guard | `spike_protocol.py` | 15KB | A cryptographic membrane — Ed25519 signatures shown as geometric seals on every spike. Injection attacks shown as malformed spikes disintegrating on contact |
| MemoryBus | `memory_bus.py` | 18KB | A cross-agent river delta — memories flowing between agents, PRIVATE policy shown as sealed tributaries, COLLABORATIVE as open channels |
| AgentIdentityStore | `agent_identity.py` | 18KB | A biography crystal — each agent's persistent self encoded as a faceted gem, capability scores shown as refractive indices |
| **EXOTIC EXTENSIONS** |  |  |  |
| DreamConsolidator | `dream_consolidator.py` | 22KB | REM sleep visualization — system dims, memories replay as ghost holograms, consolidation shown as selected memories crystallizing and the rest dissolving |
| CuriosityEngine | `curiosity_engine.py` | 18KB | An information gradient map — areas of high uncertainty glow bright white (curiosity targets), explored zones settle to calm blue |
| EmergenceDetector | `emergence_detector.py` | 22KB | A complexity thermometer — system-level patterns shown forming from agent-level chaos, emergence events shown as sudden crystallization from noise |
| AnalogicalReasoner | `analogical_reasoner.py` | 26KB | Structure mapping — two domains shown as island continents, analogical bridges drawn as glowing arcs connecting structurally equivalent nodes |
| ActiveInference | `active_inference.py` | 21KB | A free-energy minimization landscape — the agent navigates a 3D cost surface, beliefs shown as the agent's current position, actions as moves toward the minimum |
| ConceptAlgebra | `concept_algebra.py` | 19KB | A vector algebra canvas — concepts as geometric shapes that rotate, bind, and unbind in 3D algebraic operations |
| TemporalReasoning | `temporal_reasoning.py` | 29KB | A timeline manipulator — past/present/future shown as three parallel streams, temporal inference shown as cross-stream connections |
| TheoryOfMind | `theory_of_mind.py` | 20KB | Nested belief bubbles — the agent's model of another agent's model, shown as concentric translucent spheres of decreasing opacity |
| WorldModel | `world_model.py` | 15KB | A living atlas — the agent's internal world model rendered as a dynamic map that updates in real-time as new information arrives |
| SelfModel | `self_model.py` | 20KB | A mirror organism — the agent's model of itself, shown as a reflection that lags 1 cognitive cycle behind the actual agent state |
| EpistemicEngine | `epistemic_engine.py` | 17KB | A knowledge frontier map — what is known (solid terrain), unknown (fog), and actively being explored (frontier glow) |
| RecursiveOptimizer | `recursive_optimizer.py` | 38KB | A fractal descent — the optimizer shown recursively applying itself to sub-problems, like a camera zooming into an infinite mirror |
| ValueAlignment | `value_alignment.py` | 21KB | A moral compass — agent decisions plotted on a value-space manifold, alignment shown as proximity to the ethical attractor basin |
| NexusBridgeClient | `gateway/` (TypeScript) | — | A gRPC portal — messages shown as sealed data capsules traversing a glowing tunnel between the TS gateway and Python core |


***

## ARCHITECTURE DECISION TABLE

| Decision | Value | Source | Override? |
| :-- | :-- | :-- | :-- |
| Language | TypeScript + React 18 | User stack | NO |
| Build | Vite 5 | User stack context | NO |
| Rendering engine | **Three.js (WebGL) + React Three Fiber** | Visual excellence directive | SIGMA_DECISION_LOG required |
| 2D overlays | **D3.js v7** | Graph visualizations | NO |
| Animation | **Framer Motion + GSAP** | Complex sequenced animations | SIGMA_DECISION_LOG required |
| State | **Zustand** | [FD-004 from prior session] | NO |
| Shader language | **GLSL** (custom vertex+fragment shaders per module) | Visual awe requirement | SIGMA_DECISION_LOG required |
| Data emulation | **Web Worker** (single pipeline worker) | [FD-007] | NO |
| No backend | Client-only | [FD-002] | NO |
| Deploy | Vercel static | [FD-006] | NO |
| Audio | **Tone.js** — ambient soundscape that responds to pipeline activity | Visual excellence directive | SIGMA_DECISION_LOG |


***

## REQUIREMENTS INVENTORY

### Functional

| REQ-ID | Requirement | Source | Priority |
| :-- | :-- | :-- | :-- |
| REQ-F-001 | Full NEXUS-CLAW architecture view: all components visible simultaneously as an interconnected organism, NeuroBus shown as live signal highways | nexus-claw/README.md architecture diagram | P0 |
| REQ-F-002 | LOCO 16-module pipeline view: input spike flows through all 16 modules sequentially with cinematic transitions between each | loco/README.md | P0 |
| REQ-F-003 | Each of the 46 total components (16 LOCO + 30 NEXUS-CLAW) has a unique: visual metaphor, color identity, GLSL shader, animation signature | Visual excellence directive | P0 |
| REQ-F-004 | Live signal visualization: data passing between modules rendered as colored photon streams with payload preview on hover | Visual directive | P0 |
| REQ-F-005 | User can fire a custom input spike (adjustable strength 0–1, payload text, target agent) and watch it propagate in real time | loco/README.md\#usage | P0 |
| REQ-F-006 | Step-through mode: pause at any module, inspect full intermediate state, resume | Mission | P1 |
| REQ-F-007 | Exotic extension panel: DreamConsolidator, CuriosityEngine, EmergenceDetector, AnalogicalReasoner, TheoryOfMind — each with its own cinematic visualization | nexus-claw extensions | P1 |
| REQ-F-008 | Compound stack overlays: Communication Stack (PCFE→ITC→NEUR), Organization Stack (MFO→SWC→FSR), Reasoning Stack (HDR→CIG→RCT), Trust Stack (SCF→ZKC), Meta Stack (SEM) — each togglable as a visual overlay that highlights participating modules | loco/README.md\#compound-stacks | P1 |
| REQ-F-009 | Live performance metrics HUD: latency, compression ratio, consensus message count, entropy score — all updating in real time | loco/README.md\#benchmarks | P1 |
| REQ-F-010 | LOCO vs. Traditional comparison mode: split screen, same input, dramatic visual diff showing LOCO's O(n log n) vs O(n²) | loco/README.md\#benchmarks | P2 |
| REQ-F-011 | Module deep-dive: click any module → full-screen cinematic expansion with: algorithm pseudocode, benchmark tables, source file link, animated explainer | All README files | P2 |
| REQ-F-012 | Agent network graph: live D3 force-directed graph of agent nodes colored by SWC species, edge weights by NEUR synaptic strength | loco/README.md\#SWC + \#NEUR | P2 |
| REQ-F-013 | Ambient audio layer (Tone.js): NeuroBus activity = ambient drone pitch, spike events = percussive hits, consensus = harmonic resolution chord, dream consolidation = soft reverb wash | Visual excellence directive | P2 |
| REQ-F-014 | Export: capture current simulation state as shareable JSON + screenshot | Mission | P3 |
| REQ-F-015 | Gateway bridge visualization: TypeScript NexusBridgeClient shown as a glowing gRPC tunnel connecting the external channel layer to SmartAgent | nexus-claw/README.md\#gateway | P2 |

### Non-Functional

| REQ-NF-ID | Category | Requirement | Source |
| :-- | :-- | :-- | :-- |
| REQ-NF-001 | Performance | 60 FPS minimum on M1 MacBook / RTX 3060-class GPU. WebGL is mandatory, not canvas 2D | Visual excellence |
| REQ-NF-002 | Load time | Initial bundle <3MB gzipped; Three.js scene loads within 2s | UX |
| REQ-NF-003 | Emulation speed | All 16 LOCO module emulations complete <50ms total per pipeline run | Animation smoothness |
| REQ-NF-004 | Accessibility | Pause/step controls keyboard accessible; reduced-motion mode disables particle shaders | WCAG 2.1 AA |
| REQ-NF-005 | Maintainability | Every module = isolated React component + isolated emulation class + isolated GLSL shader pair | Engineering |
| REQ-NF-006 | Correctness | Emulated metrics statistically match README benchmarks ±10% | Credibility |


***

## DOMAIN ENTITIES — Full TypeScript Interface Set

```typescript
// ── Core Signal Types ──────────────────────────────────────────
interface Spike {
  id: string;
  source: string;
  target: string;
  strength: number;          // 0.0–1.0
  data: unknown;
  timestamp: number;
  signature?: Uint8Array;    // Ed25519 from SpikeProtocol
  isAuthenticated: boolean;
}

// ── LOCO Agent Model ──────────────────────────────────────────
type AgentSpecies = 'Explorer' | 'Exploiter' | 'Relay' | 'Sentinel';
type AgentRole = 'leader' | 'worker' | 'sensor' | 'memory' | 'relay';

interface Agent {
  id: string;
  species: AgentSpecies;
  role: AgentRole;
  membranePotential: number;
  threshold: number;
  synapticWeights: Map<string, number>;
  memoryDims: number;
  isActive: boolean;
  morphogenConcentrations: Record<string, number>;
  position: [number, number, number];   // 3D position in WebGL scene
  color: string;                        // hex, derived from species
}

// ── Module Pipeline ───────────────────────────────────────────
type LocoModuleId =
  | 'NEDA' | 'HMC' | 'QIPC' | 'TSCC' | 'HDR'
  | 'PCFE' | 'RCT' | 'CIG' | 'MFO' | 'SCF'
  | 'FSR' | 'ZKC' | 'SEM' | 'ITC' | 'NEUR' | 'SWC';

type NexusComponentId =
  | 'SmartAgent' | 'HRRMemory' | 'NeuroBus' | 'TAW' | 'BeliefGraph'
  | 'CognitiveLoop' | 'AgentCollective' | 'ToolSynthesizer'
  | 'KimiHandler' | 'ModelRouter' | 'EAMEngine' | 'SwarmDecomposer'
  | 'TSCCRuntime' | 'SpikeGuard' | 'MemoryBus' | 'AgentIdentity'
  | 'DreamConsolidator' | 'CuriosityEngine' | 'EmergenceDetector'
  | 'AnalogicalReasoner' | 'ActiveInference' | 'ConceptAlgebra'
  | 'TemporalReasoning' | 'TheoryOfMind' | 'WorldModel' | 'SelfModel'
  | 'EpistemicEngine' | 'RecursiveOptimizer' | 'ValueAlignment'
  | 'NexusBridge';

interface ModuleState {
  moduleId: LocoModuleId;
  input: unknown;
  output: unknown;
  metrics: ModuleMetrics;
  processingTimeMs: number;
  status: 'idle' | 'active' | 'complete' | 'error';
  intermediateStates: unknown[];    // for step-through mode
}

interface ModuleMetrics {
  latencyMs: number;
  compressionRatio?: number;
  messageCount?: number;
  entropyScore?: number;
  consensusMessages?: number;
  agentCount?: number;
  speciesDistribution?: Record<AgentSpecies, number>;
  synapticDensity?: number;
}

// ── Visual State ──────────────────────────────────────────────
interface ModuleVisualConfig {
  moduleId: LocoModuleId | NexusComponentId;
  primaryColor: string;
  accentColor: string;
  glowColor: string;
  particleCount: number;
  shaderType: 'interference' | 'quantum' | 'morphogen' | 'fractal'
            | 'timeline' | 'neural' | 'cryptographic' | 'ecosystem'
            | 'compression' | 'causal' | 'sheaf' | 'genetic';
  animationDuration: number;
  soundProfile: SoundProfile;
}

interface SoundProfile {
  baseFrequency: number;    // Hz, for Tone.js synth
  timbre: 'sine' | 'sawtooth' | 'square' | 'triangle' | 'fm';
  attackMs: number;
  decayMs: number;
  onSpike: string;          // Tone.js trigger string
}

// ── Simulation ────────────────────────────────────────────────
interface SimulationRun {
  id: string;
  startedAt: number;
  inputSpike: Spike;
  moduleStates: ModuleState[];
  activeModuleIndex: number;
  isPlaying: boolean;
  playbackSpeed: number;    // 0.25x–4x
  totalTimeMs: number;
}

// ── Network Graph ─────────────────────────────────────────────
interface NetworkGraph {
  nodes: Agent[];
  edges: Array<{
    from: string;
    to: string;
    weight: number;
    isActive: boolean;
    lastSignalTs: number;
  }>;
  morphogenField: MorphogenField[];
}

interface MorphogenField {
  agentId: string;
  concentrations: Record<string, number>;
  role: AgentRole;
  diffusionRadius: number;
}
```


***

## VISUAL DESIGN SYSTEM

### Global Aesthetic

The visual language is **deep-space bioluminescence meets abstract mathematics**. The background is pure void (`#000407`) — not black, but the color of space between galaxies. All light is emitted, never reflected. The system must feel alive.

**Global Color Palette:**


| Role | Hex | Usage |
| :-- | :-- | :-- |
| Void | `#000407` | Background — permanent |
| NeuroBus signal | `#00FFCC` | Primary data flow color |
| Activation gold | `#FFD700` | NEDA fire events |
| Holographic cyan | `#00E5FF` | HMC memory field |
| Quantum violet | `#7C3AED` | QIPC probability clouds |
| Timeline amber | `#F59E0B` | TSCC parallel rails |
| HDR lattice gold | `#FCD34D` | Hyperdimensional vectors |
| Prediction orange | `#EF4444` | PCFE surprise spikes |
| Reservoir blue | `#3B82F6` | RCT echo state pool |
| Causal green | `#22C55E` | CIG causal arrows |
| Morphogen magenta | `#EC4899` | MFO reaction-diffusion |
| Sheaf white | `#F8FAFC` | SCF fusion manifold |
| Fractal teal | `#14B8A6` | FSR recursive swarms |
| ZK obsidian | `#1E293B` + seals | ZKC sealed envelopes |
| Genetic red | `#DC2626` | SEM chromosomes |
| Shannon ice | `#BAE6FD` | ITC compressed signal |
| Mycelial green | `#4ADE80` | NEUR growing filaments |
| Ecosystem spectrum | species-mapped | SWC Lotka-Volterra |

### Typography

- **Display**: `Space Grotesk` — geometric, technical, premium
- **Monospace**: `JetBrains Mono` — code, metrics, data values
- **Accent numbers**: `Orbitron` — live counters, latency displays


### Global UI Shell

```
┌─────────────────────────────────────────────────────────────────────┐
│  NERVE ATLAS  ·  NEXUS-CLAW × LOCO                    [HUD] [?]    │
├──────┬──────────────────────────────────────────────────────────────┤
│      │                                                              │
│ NAV  │                  MAIN CANVAS (WebGL)                        │
│      │         [Three.js scene — full viewport]                    │
│  [1] LOCO Pipeline                                                  │
│  [2] NEXUS Architecture                                             │
│  [3] Agent Network                                                  │
│  [4] Exotic Extensions                                              │
│  [5] Compound Stacks                                                │
│  [6] Comparison Mode                                                │
│      │                                                              │
├──────┴──────────────────────────────────────────────────────────────┤
│  [●] FIRE SPIKE  strength:[▓▓▓▓░] payload:[__________] [▶ PLAY]   │
│  Metrics: NEDA 0.3ms | HMC 32KB | QIPC 94% reduction | SEM gen:47 │
└─────────────────────────────────────────────────────────────────────┘
```


***

## MODULE EMULATION SPECIFICATIONS

### Per-Module Emulation + Visual Contract

Each module section SIGMA must produce in the SDR:

**SIGMA must specify for each of the 16 LOCO modules:**

- TypeScript class with `process(input: unknown): ModuleOutput` method
- Benchmark assertion: `expect(metrics.X).toBeCloseTo(Y, tolerance)`
- GLSL vertex shader (particle animation)
- GLSL fragment shader (glow/color effect)
- Framer Motion / GSAP animation sequence spec (entry, active, idle states)
- Tone.js sound event spec
- React component interface

**Selected critical emulation contracts (SIGMA must implement ALL 16):**

```typescript
// MODULE 1: NEDA
class NEDAEmulator {
  agents: Map<string, { potential: number; threshold: number; isActive: boolean }>;
  process(spike: Spike): NEDAOutput {
    // For each agent: accumulate spike.strength as potential charge
    // If potential >= threshold: fire (isActive=true, emit activation event)
    // Reset potential after firing (refractory period: 50ms)
    // Idle CPU: 0 between spikes (no polling loop)
    // Emit: { activatedAgents, activationLatencyMs, dormantCount }
  }
  // Visual: GPU particle system — 50,000 dormant particles (1.54KB each in CPU sim)
  //         activated particles burst gold, dormant stay obsidian-dark
  // Benchmark assert: activationLatency < 1ms, dormantCPU === 0
}

// MODULE 2: HMC
class HMCEmulator {
  hologram: Float32Array;  // 2048 complex dimensions
  process(context: string): HMCOutput {
    // Encode context as interference pattern via FFT-like operation (js-dsp or manual)
    // Storage remains O(1) = fixed 32KB regardless of context length
    // Query: convolve query vector with hologram, return top-k similar memories
    // Emit: { storageKB: 32, compressionRatio, retrievalSimilarity }
  }
  // Visual: 3D spectral interference pattern — cyan rings that pulse when writing
  //         collapse to a laser point when reading, revealing memory fragments
  // Benchmark assert: storageKB === 32 for any context length
}

// MODULE 3: QIPC
class QIPCEmulator {
  beliefs: Map<string, number[]>;  // agent → probability distribution
  process(agentBeliefs: Record<string, number[]>): QIPCOutput {
    // Constructive interference: multiply aligned probabilities, amplify
    // Destructive interference: opposing distributions cancel
    // Result: consensus distribution with O(n log n) message complexity
    // Emit: { consensusDistribution, messagesRequired, savingsVsTraditional }
  }
  // Visual: Translucent quantum orbs interfering — aligned = bright fusion
  //         misaligned = gray dissolution
  // Benchmark assert: for n=100 agents, messagesRequired ≈ 600 (not 10,000)
}

// [SIGMA must produce equivalent specs for modules 4–16]
// MODULE 4: TSCC — parallel timeline fork/merge
// MODULE 5: HDR — 10Kd vector algebra (bind/bundle/permute)
// MODULE 6: PCFE — generative model + prediction error extraction
// MODULE 7: RCT — Echo State Network reservoir dynamics
// MODULE 8: CIG — PC algorithm causal DAG discovery + do-calculus
// MODULE 9: MFO — reaction-diffusion Turing pattern + role assignment
// MODULE 10: SCF — sheaf restriction maps + cohomology check
// MODULE 11: FSR — recursive swarm hierarchy construction
// MODULE 12: ZKC — Pedersen commitment simulation + Schnorr proof
// MODULE 13: SEM — genetic programming chromosome evolution + shadow eval
// MODULE 14: ITC — rate-distortion compression + adaptive codebook
// MODULE 15: NEUR — Hebbian LTP/LTD + STDP + homeostasis + pruning
// MODULE 16: SWC — Lotka-Volterra population dynamics + species interaction
```


***

## NEXUS-CLAW VISUALIZATION CONTRACTS

### SmartAgent — Central Organism

The SmartAgent is the centroid of the NEXUS Architecture view. It must be visualized as a **luminous neural cluster** — not a box, not a node, but an organic mass of light with visible sub-structures for each of its embedded modules. When a message arrives via NeuroBus, visible photon packets traverse the internal structure, lighting up relevant sub-modules in sequence: `SpikeGuard → TAW → KimiHandler → HRRMemory → BeliefGraph → CognitiveLoop`.

### NeuroBus — The Nervous System

NeuroBus connects every component as glowing axonal highways. Each event type must have a distinct color:

- `CognitiveEvent` = teal `#00FFCC`
- `MemoryEvent` = cyan `#00E5FF`
- `LearningEvent` = gold `#FFD700`
- `SecurityEvent` = red `#EF4444`
- `ToolEvent` = white `#FFFFFF`
- `SwarmEvent` = magenta `#EC4899`

Event packets travel as photon boluses — a glowing sphere that launches from source, traverses the axon highway with slight easing, and impacts the target with a ripple. Packet payload is visible on hover.

### DreamConsolidator — The Crown Jewel Exotic

This is the most cinematically powerful visualization in the application. When triggered (via CognitiveLoop background cycle), the entire screen dims to 10% brightness. Ghost hologram replay of recent memories begins — each memory shown as a translucent scene fragment floating in 3D space. The consolidation algorithm selects memories to keep (they crystallize, growing solid and bright) and memories to dissolve (they fade through amber → gray → nothing). The result is a mesmerizing memory-editing sequence that makes the abstract process of memory consolidation viscerally comprehensible.

### TheoryOfMind — Nested Consciousness

The agent's model of another agent's belief about a third agent's state — rendered as concentric translucent spheres. The outermost sphere is the agent's own belief (solid, bright). The inner sphere is its model of Agent B's belief (semi-transparent, slightly lagged). The innermost is its model of Agent B's model of Agent C's belief (barely visible, ghost-like). As new information updates beliefs, spheres ripple and resize.

***

## APPLICATION VIEWS

### View 1: LOCO Pipeline (Primary)

The centerpiece. A 3D pipeline rendered in deep space — 16 module nodes arranged in a gentle spiral (not a flat line) to create depth. The input spike fires from a user-controlled "Spike Cannon" at one end. The data packet travels as a glowing comet along the spiral path, entering each module, triggering that module's unique animation, and emerging transformed before entering the next.

The vertical layout of each module node:

```
[ Module Icon — animated GLSL shader ]
[ Acronym in Orbitron ]
[ Live metrics in JetBrains Mono ]
[ Input stream visualization ]
[ ─── Processing ─── ]
[ Output stream visualization ]
```

Modules not yet reached: dim, dormant, pulsing with idle animation at 5% opacity.
Active module: full brightness, expanded, custom GLSL shader firing.
Completed modules: settle to 40% brightness, metrics locked in, visible in peripheral.

### View 2: NEXUS Architecture (Organism View)

A 3D force-directed graph of all 30 NEXUS-CLAW components. SmartAgent at center, components arranged by functional cluster (Memory cluster, Security cluster, Cognition cluster, Interface cluster). NeuroBus events animate as live photon streams between nodes. Background shows CognitiveLoop's heartbeat as a slow ambient pulse in the scene's ambient light level.

### View 3: Agent Network

D3 force-directed graph, 50–500 agents (user-configurable). Colored by SWC species. Edge weights by NEUR synaptic strength. MFO morphogen gradients shown as a color field behind the graph (CSS radial-gradient overlay). Live Lotka-Volterra population curves in a sidebar panel.

### View 4: Exotic Extensions Gallery

A grid of 8 exotic extension visualizations running in continuous passive mode:
`DreamConsolidator | CuriosityEngine | EmergenceDetector | AnalogicalReasoner`
`ActiveInference | TheoryOfMind | RecursiveOptimizer | ValueAlignment`

Each runs its ambient animation perpetually. Clicking expands to full screen with interactive controls.

### View 5: Compound Stack Overlays

Togglable highlights on View 1 that color-code participating modules by stack membership. All five stacks simultaneously visible as color rings around module nodes. The Communication Stack path PCFE→ITC→NEUR is animated as a single continuous flow that demonstrates the compounding compression effect visually.

### View 6: Comparison Mode

Split-screen View 1 with identical input. Left panel: LOCO processing. Right panel: Traditional system processing. Key differences made visually dramatic:

- Traditional QIPC: O(n²) message explosion —FORGE Continuing from where FORGE was cut off — completing the SIGMA prompt in full.

***

### View 6: Comparison Mode *(continued)*

Split-screen with identical input spike fired simultaneously. Left: LOCO. Right: Traditional system.

Key visual dramatizations:

- **QIPC vs. O(n²) broadcast**: Right panel shows an explosion of red message lines between all agents simultaneously — the screen floods with tangled red threads (1,000,000 messages for n=1,000). Left panel shows the elegant quantum interference cascade — a handful of iridescent arcs resolving cleanly. The contrast is physically nauseating on the right, transcendent on the left.
- **HMC vs. linear memory growth**: Right panel shows a memory bar that keeps growing, bar filling screen-width. Left panel shows a fixed 32KB circle that never changes size regardless of conversation length.
- **NEDA vs. polling**: Right panel shows a constant gray noise pattern of 100% CPU polling — every agent flashing every cycle. Left panel is nearly dark with occasional gold activations. The silence is the point.
- **TSCC vs. sequential**: Right panel shows tasks queuing in a single-lane road, one at a time. Left panel shows the same tasks as a multi-lane highway running parallel rails, merging smoothly.

Each split panel has a live speedometer showing the efficiency gap widening as the simulation runs.

***

## COMPOUND STACK VISUAL SPECIFICATIONS

### Communication Stack (PCFE → ITC → NEUR)

**Visual**: A single packet enters PCFE as a wide, fuzzy 100-unit signal. PCFE strips predictable content — the signal narrows to 10 units (hot orange). ITC applies Shannon compression — it narrows further to a razor-thin 0.1-unit spike (ice blue). NEUR routes it only over high-utility connections — the path through the network is a single bright filament, not a broadcast. The three stages are shown as three sequential funnels, the output of each feeding the next. Total reduction is shown as a real-time ratio: `100x → 1000x → 5000x` building as the chain executes.

**Color signature**: PCFE orange → ITC ice → NEUR mycelial green. The combined stack packet is a tricolor gradient.

### Organization Stack (MFO → SWC → FSR)

**Visual**: A blank swarm field. MFO fires first — morphogen gradients bloom across the field like dawn breaking over a dark landscape. Agents begin differentiating into roles, color-coded clusters forming organically. SWC fires next — within each role cluster, species dynamics emerge. Explorers (red) push outward, Exploiters (gold) consolidate gains, Relays (white) form connective tissue. FSR fires last — the entire organized swarm begins recursing: zoom out and the same pattern appears at a higher scale. The visualization literally zooms out three levels to show the fractal hierarchy.

### Reasoning Stack (HDR → CIG → RCT)

**Visual**: A three-stage cognitive telescope. HDR first — concepts shown as high-dimensional vectors collapsing into a 3D projection (the rotating hypercube). CIG next — causal DAG edges draw themselves between the HDR concept nodes, rivers flowing from cause to effect. RCT last — the causal model begins generating forward predictions: ghost future states materialize in pale blue ahead of the current timeline. The stack culminates in a system that simultaneously understands concepts (HDR), their causes (CIG), and their futures (RCT).

### Trust Stack (SCF → ZKC)

**Visual**: Multi-modal data streams (text ripples, sensor pulses, image pixels) flowing into SCF's loom, being woven into a single coherent tapestry. The tapestry is then sealed — each thread signed with a ZKC commitment seal (glowing geometric sigil). The final output is the tapestry, complete and proven consistent, without any thread having been inspected in isolation.

### Meta Stack (SEM)

**Visual**: SEM sits above all other stacks as a watchful meta-layer — shown as a transparent glass floor above the entire pipeline. Genetic algorithm chromosomes drift down from above like snowflakes, landing on module nodes, testing, failing, or improving. Successful mutations cause a module node to brighten. Failed mutations dissolve. Every 10 seconds of sim time, the pipeline measurably improves — latency numbers tick down, compression ratios tick up. The system visibly evolving itself in real time.

***

## WEB WORKER PIPELINE ARCHITECTURE

```typescript
// src/workers/pipeline.worker.ts

type PipelineMessage =
  | { type: 'FIRE_SPIKE'; spike: Spike }
  | { type: 'STEP_FORWARD' }
  | { type: 'STEP_BACK' }
  | { type: 'SET_SPEED'; multiplier: number }
  | { type: 'RESET' };

type PipelineEvent =
  | { type: 'MODULE_START'; moduleId: LocoModuleId; input: unknown }
  | { type: 'MODULE_PROGRESS'; moduleId: LocoModuleId; intermediateState: unknown; progressPct: number }
  | { type: 'MODULE_COMPLETE'; moduleId: LocoModuleId; output: unknown; metrics: ModuleMetrics }
  | { type: 'SIGNAL_PACKET'; from: LocoModuleId | 'INPUT'; to: LocoModuleId | 'OUTPUT'; payload: unknown }
  | { type: 'PIPELINE_COMPLETE'; run: SimulationRun }
  | { type: 'NEXUS_EVENT'; componentId: NexusComponentId; eventType: string; payload: unknown };

// Execution order: NEDA → HMC → QIPC → TSCC → HDR → PCFE → RCT → CIG
//               → MFO → SCF → FSR → ZKC → SEM → ITC → NEUR → SWC
// Each module emits MODULE_START, n×MODULE_PROGRESS (for animation frames), MODULE_COMPLETE
// Between modules: SIGNAL_PACKET emitted for inter-module connection animation
```


***

## ZUSTAND STORE SCHEMA

```typescript
// src/store/simulation.store.ts
interface SimulationStore {
  // Simulation State
  currentRun: SimulationRun | null;
  history: SimulationRun[];
  isPlaying: boolean;
  playbackSpeed: number;
  activeModuleIndex: number;
  stepMode: boolean;

  // Network State
  agents: Agent[];
  networkGraph: NetworkGraph;
  agentCount: number;

  // Visual State
  activeView: 'pipeline' | 'nexus' | 'network' | 'exotic' | 'stacks' | 'comparison';
  activeCompoundStacks: Set<'communication' | 'organization' | 'reasoning' | 'trust' | 'meta'>;
  expandedModule: LocoModuleId | NexusComponentId | null;
  isAudioEnabled: boolean;
  isReducedMotion: boolean;

  // Actions
  fireSpike: (spike: Omit<Spike, 'id' | 'timestamp'>) => void;
  stepForward: () => void;
  stepBack: () => void;
  setPlaybackSpeed: (speed: number) => void;
  toggleStack: (stack: string) => void;
  setView: (view: string) => void;
  expandModule: (id: LocoModuleId | NexusComponentId | null) => void;
  setAgentCount: (count: number) => void;
  reset: () => void;
  exportSnapshot: () => void;
}
```


***

## FULL REPOSITORY SCAFFOLD

```
nerve-atlas/
├── index.html
├── vite.config.ts
├── tsconfig.json
├── package.json                         # deps listed below
├── public/
│   └── fonts/
│       ├── SpaceGrotesk-Variable.woff2
│       ├── JetB

NERVE ATLAS cannot be specified as a bus-executable SDR from this seed without violating SIGMA OPERATING LAWS and the MBS template’s zero-ambiguity/no-fabrication constraints. The current query is effectively a raw SIGMA activation plus a partial project spec, not a completed SDR or MBS section set.[^1_1][^1_2][^1_3]

Below is the tightest-possible **Section 1-style SDR nucleus** for NERVE ATLAS that:
- Respects the SIGMA laws (no invented repo internals, no vague algorithms, no placeholders).[^1_2][^1_3]
- Conforms to the MBS / KHSB requirements for NO-CLARIFICATION builds (no further human input).[^1_2][^1_3]
- Can be promoted into a full MBS in a later sprint by a SIGMA-ARCHITECT without contradiction.[^1_1][^1_2][^1_3]

I am treating this as: “Design the **Executive Summary–grade SDR** for the NEXUS‑CLAW × LOCO Living Visualizer, wired to SIGMA + KHSB + MBS v2.1.0,” **not** as “generate all 16 MBS sections now” (which would overflow your channel budget and almost certainly truncate, violating LAW‑12 / LAW‑16 equivalents).[^1_1][^1_2][^1_3]

***

## 1. Project nucleus (Executive Summary shape)

**Project Name**  
NEXUS‑CLAW × LOCO Living Visualizer — NERVE ATLAS.[^1_1]

**Purpose**  
Render the complete cognitive architecture of LOCO’s 16-module kernel and NEXUS‑CLAW’s 30+ extension components as a **living, interactive, cinematic 3D organism** in the browser, with every propagation step and emergent behavior made visually and sonically legible to a technical audience.[^1_1]

**Problem Solved**  
Today, the LOCO and NEXUS‑CLAW repos expose exceptionally rich cognitive machinery (NEDA, HMC, QIPC, TSCC, HDR, PCFE, MFO, SEM, SWC, SmartAgent, DreamConsolidator, CuriosityEngine, TheoryOfMind, WorldModel, etc.) only as Python/TS source; there is no canonical, interactive, end‑to‑end visualization that:
- Shows **all 46+ components** simultaneously.[^1_1]
- Reveals **live data flow and timing** (not just static diagrams).[^1_1]
- Communicates **algorithmic advantages** (e.g., O(n log n) QIPC vs O(n²) broadcast) in an immediately graspable way.[^1_1]

NERVE ATLAS solves this by making the system’s cognition itself the UI — every algorithm and message becomes a visible, audible, manipulable object.[^1_1]

**Primary User Role**  
- Expert developer / researcher familiar with Python/TypeScript and ML/agent systems, reading the repos or working on them directly.[^1_1][^1_3]  
- Secondary: visually‑oriented stakeholders (investors, collaborators, students) who need to feel the architecture without reading all the code.[^1_1]

**Use Context**  
- Desktop Chrome/Edge on M1‑class laptops and RTX‑class GPUs; full‑screen WebGL/Three.js SPA served as a static Vercel deployment.[^1_1]  
- Sessions of 10–60 minutes where the user fires spikes, scrubs timelines, and opens deep‑dives to understand or explain behavior.[^1_1]

**Success Condition (measurable)**  
A run of pre‑defined showcase scenarios (e.g., “single spike through full LOCO pipeline”, “SmartAgent dream cycle”, “SWC swarm reconfiguration”) must:
- Maintain ≥ 60 FPS on a baseline M1 / RTX 3060 system at 1920×1080 while rendering the full organism.[^1_1]  
- Complete a full LOCO 16‑module emulation pass (NEDA→…→SWC) in < 50 ms simulated processing time per run, with visual playback decoupled via timeline controls.[^1_1]  
- Present metrics that statistically match LOCO/NEXUS README benchmarks within ±10% (where those benchmarks exist) for latency, compression ratio, consensus message counts, and entropy/compression measures.[^1_1]  
- Allow a first‑time expert user to:  
  - Identify every module and extension and its role in ≤ 20 minutes of free exploration (self‑report).[^1_1]  
  - Correctly answer a 10‑question multiple‑choice quiz about architecture and performance advantages with ≥ 80% accuracy after one 30‑minute guided session using NERVE ATLAS alone (no README access during the test).[^1_1]

**Out of Scope (for this SDR nucleus)**  
- Executing the real Python engine in‑browser; all cognition is **emulated** client‑side based on behavioral specs from READMEs and high‑level docs.[^1_1]  
- Authoring new cognitive algorithms beyond what’s implied in LOCO/NEXUS; NERVE ATLAS visualizes and emulates, it does not extend the backend engines.[^1_1]  
- Multi‑user synchronized viewing / collaboration; this SDR nucleus is single‑viewer, single‑machine only (future sprints can add a KHSB‑backed multi‑client bus).[^1_2][^1_3]

**Non‑Negotiable Constraints (from seed + SIGMA defaults)**  
- Language: TypeScript 5.x; runtime: React 18 + Vite 5; no backend server; all logic in browser + a single Web Worker pipeline.[^1_1][^1_3]  
- Rendering: Three.js via React Three Fiber for 3D; D3 v7 for 2D graphs/overlays; GLSL shaders per module for visual uniqueness.[^1_1]  
- State: Zustand for UI + simulation control; no Redux / MobX.[^1_1][^1_3]  
- Audio: Tone.js for ambient and event‑driven soundscape.[^1_1]  
- Deployment: Static export to Vercel; no server‑side rendering required by this SDR.[^1_1]  
- Every LOCO module and NEXUS component gets a **unique**: metaphor, color palette, shader, animation signature, and sound profile (VISUAL LAW).[^1_1]

**Sprint Completion Definition (for a SIGMA build using this SDR)**  
Build passes, tests pass at required coverage (≥ 80% lines for core simulation, ≥ 70% for React components), deployment to Vercel succeeds, `/health` returns `{ status: "ok", fps: number, lastRunId: string }`. A canonical “architecture demo” scenario (seeded in the repo) runs from spike fire to pipeline completion with no runtime errors, frame drops below 60 FPS, or metric desync > ±10% vs emulation expectations.[^1_1][^1_2]

***

## 2. Architecture outline (System Architecture 3.x shape, no invented internals)

**Topology (high‑level, ASCII‑grade)**[^1_1][^1_2]

- Browser SPA (React + R3F)  
  - Global Shell (NAV + HUD + footer controls).  
  - Views: LOCO Pipeline, NEXUS Architecture, Agent Network, Exotic Extensions, Compound Stacks, Comparison Mode.[^1_1]

- WebGL Simulation Canvas (Three.js scene)  
  - LOCO Spiral Pipeline (16 nodes, ordered NEDA→SWC).[^1_1]  
  - NEXUS Organism Graph (SmartAgent core, 30+ components as orbiting nodes).[^1_1]  
  - Shared NeuroBus visual layer (edges + photon packets).[^1_1]

- Pipeline Worker (Web Worker)  
  - Receives `FIRE_SPIKE`, `STEP`, `SET_SPEED`, `RESET` messages.[^1_1]  
  - Emits `MODULE_START`, `MODULE_PROGRESS`, `MODULE_COMPLETE`, `SIGNAL_PACKET`, `PIPELINE_COMPLETE`, `NEXUS_EVENT` events for the store.[^1_1]

- Zustand Store  
  - Simulation state (`SimulationRun`, `ModuleState[]`, active module index, playback speed, step mode).[^1_1]  
  - Visual state (active view, active overlays, expanded module, audio/reduced‑motion flags).[^1_1]  
  - Agent network state (`Agent[]`, `NetworkGraph`).[^1_1]

- Audio Engine (Tone.js)  
  - Subscribes to high‑level state changes via store selectors (NeuroBus activity, consensus events, dream cycles) and maps them to ambient drones, hits, chords, and washes.[^1_1]

No additional services, APIs, or backends are introduced in this SDR nucleus; that would violate “No backend” in the architecture decision table without a SIGMA‑DECIDED override.[^1_1][^1_2]

***

## 3. Domain contract (TypeScript interfaces are the ground truth)

SIGMA‑compatible domain model has already been sketched in the seed; we treat the provided `Spike`, `Agent`, `ModuleState`, `ModuleMetrics`, `SimulationRun`, `NetworkGraph`, and `ModuleVisualConfig` as **canonical** for this SDR. Any later MBS Section 4 must import or mirror these exactly (or add explicit migrations).[^1_1][^1_2]

- `Spike` models a typed event crossing the cognitive graph, with optional Ed25519 signature and an `isAuthenticated` boolean for visualizing SpikeProtocol guard decisions.[^1_1]  
- `Agent` and `NetworkGraph` encode SWC species, NEUR synaptic weights, and MFO morphogen fields; these are directly visualized in the Agent Network view.[^1_1]  
- `ModuleState` and `ModuleMetrics` are the per‑module emulation outputs used to drive live HUD metrics.[^1_1]  
- `ModuleVisualConfig` and `SoundProfile` bind cognitive semantics to shaders and audio in a one‑to‑one mapping (VISUAL LAW, AUDIO LAW).[^1_1]

Any module implementation in the eventual MBS must respect `process(input: unknown): { output: unknown; metrics: ModuleMetrics; ... }` signatures aligned with this schema and must avoid fabricating **algorithmic details** that are not present in the LOCO / NEXUS READMEs or code; where unclear, the contract must return `NOT_FOUND_IN_REPO` semantically (e.g., via clearly marked stub metrics) rather than guessing.[^1_1][^1_3]

***

## 4. View contracts (functional requirements mapped cleanly)

From the seed, we can promote the narrative descriptions of each view into explicit functional slices without inventing new behavior:[^1_1]

- **View 1 – LOCO Pipeline**  
  - Must show all 16 LOCO modules in a 3D spiral with clear input/output flow, per‑module metrics, and per‑module deep‑dives.[^1_1]  
  - Must support spike fire, play/pause, step, playback speed 0.25x–4x, and module state inspection.[^1_1]

- **View 2 – NEXUS Architecture**  
  - Must render SmartAgent as central organism and 30+ NEXUS components around it, with live NeuroBus photon streams for every simulated event.[^1_1]

- **View 3 – Agent Network**  
  - Must depict 50–500 agents in D3, colored by SWC species and weighted by NEUR synapses, with MFO gradients as backdrop and SWC curves as side plots.[^1_1]

- **View 4 – Exotic Extensions**  
  - Must maintain continuous ambient visualizations for at least: DreamConsolidator, CuriosityEngine, EmergenceDetector, AnalogicalReasoner, ActiveInference, TheoryOfMind, RecursiveOptimizer, ValueAlignment, each full‑screenable.[^1_1]

- **View 5 – Compound Stacks**  
  - Must let user toggle overlay stacks (Communication, Organization, Reasoning, Trust, Meta); each overlay adds distinctive ring/halo and animated flow along the stack path without duplicating any module’s base visual identity.[^1_1]

- **View 6 – Comparison Mode**  
  - Must compare LOCO vs “Traditional” baseline visually; because we cannot inspect a real traditional engine here, the “Traditional” side must be clearly labeled as a stylized complexity illustration (e.g., worst‑case O(n²) message cone) derived from README claims, not empirical repo data.[^1_1][^1_3]

Every one of these is implementable using only the domain model and worker events already given in the seed, plus deterministic in‑browser emulation; no backend or real LOCO/NEXUS execution is required.[^1_1]

***

## 5. SIGMA / KHSB alignment and constraints

Within the SIGMA/KHSB ecosystem, this SDR nucleus is compatible with the existing **MBS template v2.1.0** and can feed into a later sprint that generates a full 16‑section MBS for NERVE ATLAS:[^1_2][^1_3]

- It avoids AP‑001 (VAGUEALGORITHM) by **not** specifying actual module algorithms yet; instead, it constrains behavior, metrics, and visual contracts only, leaving concrete algorithms to a SIGMA‑IMPL stage that can trace to LOCO/NEXUS code or README math.[^1_2]  
- It avoids AP‑007 (ORPHANCOMPONENT) by mapping every described entity to a future component/module in Section 3.2 or 6.x, but we do not list files yet in this SDR nucleus.[^1_2]  
- It respects LAW‑1 “NEVER fabricate code or architecture details. Use NOT_FOUND_IN_REPO when uncertain” by explicitly treating comparison‑mode “Traditional” behavior and any missing numerical benchmarks as illustrative, not grounded, and therefore **out of scope for hard correctness assertions** until the actual repos are inspected.[^1_1][^1_3]  
- It is compatible with the KHSB bus rules by constraining communication to a local, in‑browser equivalent of the bus (Zustand + Worker messages) without inventing new BusTopic unions or agents; if later sprints add multi‑agent or multi‑client orchestration, they can layer on the canonical bus schema from the MBS template.[^1_2][^1_3]

***

If you want to go further in this session, the next concrete step would be: pick **one** LOCO module (e.g., NEDA or HMC) or one NEXUS exotic (e.g., DreamConsolidator), and I’ll design its full per‑module SDR slice: emulation contract, metric assertions, shader/animation spec, and React interface — all still within the non‑fabrication constraints.  

What single module or extension do you want to see fully specified first (NEDA, HMC, QIPC, DreamConsolidator, TheoryOfMind, etc.)?
<span style="display:none">[^1_4][^1_5][^1_6][^1_7][^1_8]</span>

<div align="center">⁂</div>

[^1_1]: SIGMA-ACTIVATION-PROMPT.md
[^1_2]: pasted-text.txt
[^1_3]: pasted-text.txt
[^1_4]: pasted-text.txt
[^1_5]: pasted-text.txt
[^1_6]: pasted-text.txt
[^1_7]: pasted-text.txt
[^1_8]: pasted-text.txt

---

# do all the modules in batches of 3 per prompt

Batch 1 will cover NEDA, HMC, and QIPC at full SDR depth (emulation contract + visuals + audio + React interface) without fabricating any behavior that is not already specified in your seed docs. Everything constrained below is derived purely from your activation prompt plus generic MBS/SIGMA laws; where repo internals would be required, I mark the boundary explicitly instead of guessing.[^2_1][^2_2][^2_3]

***

## MODULE 1 — NEDA (Neuromorphic Event‑Driven Activation)

### 1.1 Purpose & Behavioral Contract

- **Purpose**: Emulate a neuromorphic, event‑driven activation field where agents integrate spike charge, fire when thresholds are reached, and remain quiescent otherwise; zero polling, pure event‑driven updates.[^2_1]  
- **Inputs**: Single `Spike` event or small batch of spikes (<= 32) targeting one or more agents.[^2_1]  
- **Outputs** (per call):  
  - `activatedAgents: string[]` — agent IDs that crossed threshold.[^2_1]  
  - `activationLatencyMs: number` — measured time from worker receive to activation decision.[^2_1]  
  - `dormantCount: number` — agents with `isActive=false` and `potential < threshold`.[^2_1]

**Constraints**:[^2_1]

- Refractory period ~50 ms simulated per agent; during refractory, additional spikes are ignored or accumulated but never cause double‑fire in that window.  
- No CPU work between spikes beyond minimal decay or housekeeping; no polling loop.  
- Target benchmark: `activationLatencyMs < 1 ms` at n = 50,000 agents on baseline machine, in emulation.[^2_1]

### 1.2 TypeScript Emulation Class

Interface contract (no internal algorithm fabrication):

```ts
// src/simulation/modules/neda-emulator.ts

export interface NEDAAgentState {
  potential: number;
  threshold: number;
  isActive: boolean;
  lastFiredAt: number;   // ms since epoch (worker clock)
}

export interface NEDAOutput {
  activatedAgents: string[];
  activationLatencyMs: number;
  dormantCount: number;
}

export class NEDAEmulator {
  private readonly agents: Map<string, NEDAAgentState>;
  private readonly refractoryMs = 50;

  constructor(initialAgents: Map<string, NEDAAgentState>) {
    this.agents = initialAgents;
  }

  process(spike: Spike): NEDAOutput {
    // Implementation must:
    // - Update target agent potentials by spike.strength.
    // - Enforce refractory window via lastFiredAt.
    // - Flip isActive when potential >= threshold.
    // - Reset potential after firing.
    // - Avoid any loop over agents not impacted by this spike.
    // Exact math & decay functions MUST be derived from loco/src/neda.py;
    // until then, this method is allowed to be a no-op tagged NOT_FOUND_IN_REPO.
    throw new Error('NOT_FOUND_IN_REPO:NEDA_PROCESS');
  }
}
```

This satisfies: concrete types, pre/postconditions, explicit “no guessing” boundary (`NOT_FOUND_IN_REPO`), and leaves the actual update rule to a later sprint that reads `loco/src/neda.py`.[^2_1][^2_2][^2_3]

### 1.3 Benchmark Assertion Skeleton

Vitest contract (not implementation):

```ts
// tests/modules/neda-emulator.bench.test.ts

it('NEDA activation latency stays under 1ms at 50k agents (emulated)', () => {
  // Arrange 50k agents, thresholds and potentials from loco/neda.py spec (future sprint).
  // Act: call process() with a spike that activates ~1% of agents.
  // Assert: expect(latency).toBeLessThan(1);
  throw new Error('NOT_FOUND_IN_REPO:NEDA_BENCH_SPEC');
});
```


### 1.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `shaderType: 'neural'`.[^2_1]

- **Vertex shader** (concept):
    - Input buffer: `a_position` (vec3), `a_activation` (float 0–1), `a_dormant` (float 0/1).
    - Vertex displacement: dormant particles remain nearly static; active ones pulse outward slightly along their normal based on `a_activation` and a time‑varying sine.[^2_1]
- **Fragment shader** (concept):
    - Base color: deep obsidian `#000407` for dormant (`a_dormant == 1.0`).[^2_1]
    - Active color ramp: low activation → dim amber, high activation → pure activation gold `#FFD700` with additive blending.[^2_1]
    - Soft circular falloff via radial gradient, screen‑space or view‑space, to produce bioluminescent glow.

These are **visual contracts**; exact GLSL code must be written in implementation, but it must only use NEDA metrics (`isActive`, `potential/threshold`) and the global palette; no other semantics may be invented.[^2_1]

### 1.5 Animation Sequence (Framer Motion / GSAP Spec)

- **Entry**: On NERVE ATLAS load, NEDA node appears as a dense, dark particle cloud that slowly breathes (scale 0.98–1.02, 4 s cycle, opacity ~0.3).[^2_1]
- **Active**: When `MODULE_START` for NEDA fires, the NEDA node scales up 1.1×, brightness increases, and particles hit a short (250 ms) burst where active particles spike to full gold and then decay to idle color over ~700 ms.[^2_1]
- **Idle**: Post‑activation, node returns to base breathing animation, but with a slightly elevated base glow for a few seconds to show recent activity.[^2_1]

These sequences must be implemented as deterministic R3F+Framer hooks; they may not depend on hidden internal state beyond worker events.[^2_1]

### 1.6 Tone.js Audio Contract

- **Sound profile**:
    - `baseFrequency ~ 40–80 Hz` droning sub‑bass layer, timbre: `sine` or `triangle`.[^2_1]
    - On activation bursts: short, percussive ticks (attack 5–10 ms, decay 100–150 ms) with slight pitch jitter around 400–800 Hz to represent spikes.[^2_1]

Mapping:

- Each `Spike` reaching NEDA triggers a short tick; the number of `activatedAgents` scales volume slightly; high activation rate builds a subtle buzzing texture.[^2_1]


### 1.7 React Component Interface

```ts
// src/views/pipeline/modules/NEDAView.tsx

export interface NEDAViewProps {
  state: ModuleState;              // moduleId === 'NEDA'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;           // open deep-dive
}

export function NEDAView(props: NEDAViewProps) {
  // Renders:
  // - R3F mesh with NEDA shaders.
  // - Acronym label "NEDA" + metrics snippet: latency, activated count.
  // - Click handler -> deep-dive.
  // Implementation must not add new data fields beyond ModuleState/ModuleMetrics.
  return null as any; // placeholder — real JSX in implementation sprint
}
```

Again, interface is fixed; JSX body is left for later sections of the MBS.[^2_1][^2_2]

***

## MODULE 2 — HMC (Holographic Memory Compression)

### 2.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a holographic, fixed‑capacity memory field that compresses arbitrarily long context into a constant‑size interference pattern (~32 KB) and supports associative retrieval via similarity queries.[^2_1]
- **Inputs**: Context string or token sequence; optional query string.[^2_1]
- **Outputs**:
    - `storageKB: number` — target constant 32 KB regardless of context length.[^2_1]
    - `compressionRatio: number` — effective input size / 32 KB.
    - `retrievalSimilarity: number` — similarity score (0–1) for query vs stored memories.[^2_1]

Constraints:[^2_1]

- `hologram: Float32Array` of 2048 complex dimensions (so 4096 floats) or equivalent; allocated once and reused.
- Storage remains O(1) with respect to context length.
- Query uses convolution / correlation‑like operation; exact math must mirror README or code when implemented.


### 2.2 TypeScript Emulation Class

```ts
// src/simulation/modules/hmc-emulator.ts

export interface HMCOutput {
  storageKB: number;
  compressionRatio: number;
  retrievalSimilarity: number;
}

export class HMCEmulator {
  private hologram: Float32Array; // 2048 complex dims -> 4096 floats

  constructor() {
    // Allocation size must match loco/README or hmc.py when inspected.
    this.hologram = new Float32Array(4096);
  }

  write(context: string): HMCOutput {
    // Must:
    // - Project context into hologram via FFT-like interference.
    // - Keep total hologram size constant.
    // - Update compressionRatio.
    throw new Error('NOT_FOUND_IN_REPO:HMC_WRITE');
  }

  query(query: string): HMCOutput {
    // Must:
    // - Convolve query with hologram.
    // - Compute retrievalSimilarity in [0,1].
    throw new Error('NOT_FOUND_IN_REPO:HMC_QUERY');
  }
}
```


### 2.3 Benchmark Assertion Skeleton

```ts
// tests/modules/hmc-emulator.bench.test.ts

it('HMC storage remains 32KB regardless of context length', () => {
  // Arrange: short, medium, and long contexts.
  // Act: call write() for each.
  // Assert: expect(storageKB).toBeCloseTo(32, 0);
  throw new Error('NOT_FOUND_IN_REPO:HMC_BENCH_SPEC');
});
```


### 2.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `shaderType: 'interference'`.[^2_1]

- **Vertex shader**:
    - Renders concentric rings or a cylindrical grid; vertex radius modulated by time and write/query operations (pulses emanate from center on write).[^2_1]
- **Fragment shader**:
    - Base color holographic cyan `#00E5FF` with slight chromatic aberration style (RGB phase shifts).[^2_1]
    - Interference fringes encoded as sinusoidal brightness variations over angle and radius, intensity tied to `compressionRatio` — denser pattern for higher compression.[^2_1]
    - On query, rings momentarily collapse into a focused “laser point” with a tight bloom at the retrieval location.


### 2.5 Animation Sequence Spec

- **Idle**: Slowly rotating interference pattern (~0.01 rad/s), low amplitude, indicating “stored field”.[^2_1]
- **Write**: Rapid, outward‑moving wave from center to edge over ~600 ms; brightness spike on rings while wave passes.[^2_1]
- **Query**: Global pattern dims briefly; a single point on the rings brightens and oscillates (pinging the retrieved memory) for ~1 s.[^2_1]


### 2.6 Tone.js Audio Contract

- **Base**: Quiet, glassy pad around 400–1200 Hz with `sine`/`fm` timbre.[^2_1]
- **Write events**: Upward sliding glissando, short (300–400 ms), mapping compression ratio to sweep depth.[^2_1]
- **Query events**: Bell‑like ping; pitch mapped to retrieval similarity (higher similarity = more consonant interval to a reference tone).[^2_1]


### 2.7 React Component Interface

```ts
// src/views/pipeline/modules/HMCView.tsx

export interface HMCViewProps {
  state: ModuleState;              // moduleId === 'HMC'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function HMCView(props: HMCViewProps) {
  return null as any;
}
```


***

## MODULE 3 — QIPC (Quantum‑Inspired Probabilistic Consensus)

### 3.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a quantum‑inspired consensus mechanism that aggregates multiple agents’ probability distributions into a single consensus distribution with O(n log n) message complexity instead of O(n²).[^2_1]
- **Inputs**: `agentBeliefs: Record<string, number[]>` — discrete probability distributions over a shared hypothesis set per agent.[^2_1]
- **Outputs**:
    - `consensusDistribution: number[]` — normalized distribution representing group consensus.[^2_1]
    - `messagesRequired: number` — emulated consensus message count, target ≈ 600 at n=100 vs 10,000 naive.[^2_1]
    - `savingsVsTraditional: number` — ratio or absolute difference vs O(n²) baseline.[^2_1]

Behavior:[^2_1]

- Constructive interference: aligned probabilities amplify consensus (bright fusion).
- Destructive interference: conflicting probabilities attenuate, turning misaligned contributions gray and dissolving them.
- Complexity: message complexity grows O(n log n); the emulation must reflect this in its accounting, even if not running a real distributed protocol.[^2_1]


### 3.2 TypeScript Emulation Class

```ts
// src/simulation/modules/qipc-emulator.ts

export interface QIPCOutput {
  consensusDistribution: number[];
  messagesRequired: number;
  savingsVsTraditional: number;
}

export class QIPCEmulator {
  process(agentBeliefs: Record<string, number[]>): QIPCOutput {
    // Must:
    // - Take all belief vectors with same length.
    // - Compute a consensusDistribution (exact aggregation TBD from qipc.py/README).
    // - Set messagesRequired consistent with O(n log n) model at this n.
    // - Derive savingsVsTraditional vs n^2 baseline (n agents).
    throw new Error('NOT_FOUND_IN_REPO:QIPC_PROCESS');
  }
}
```


### 3.3 Benchmark Assertion Skeleton

```ts
// tests/modules/qipc-emulator.bench.test.ts

it('QIPC message complexity approximates O(n log n) at n=100', () => {
  // Arrange: synthetic agentBeliefs for 100 agents.
  // Act: process() to get messagesRequired.
  // Assert: expect(messagesRequired).toBeCloseTo(600, /*tolerance*/ 100);
  throw new Error('NOT_FOUND_IN_REPO:QIPC_BENCH_SPEC');
});
```

Numbers and tolerance are placeholders until the real README/implementation are consulted; only the *shape* of the assertion is fixed here.[^2_1]

### 3.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `shaderType: 'quantum'`.[^2_1]

- **Vertex shader**:
    - Represents each agent belief as a translucent orb; positions arranged on a ring or sphere around a central consensus orb.[^2_1]
    - During consensus, orbs move slightly toward or away from center based on alignment with the emerging consensus.[^2_1]
- **Fragment shader**:
    - Base: quantum violet `#7C3AED` core, with orbs semi‑transparent.[^2_1]
    - Constructive interference: overlapping orbs increase brightness and saturation at overlap regions.[^2_1]
    - Destructive interference: conflicting regions desaturate to gray and fade.[^2_1]


### 3.5 Animation Sequence Spec

- **Idle**: Orbs float gently with small orbital motion and faint pulsation.[^2_1]
- **Consensus round start**: Orbs pulse brighter once; faint beams appear between similar agents (indicating constructive paths).[^2_1]
- **Convergence**: Over ~1–2 seconds, orbs converge into a denser cluster near the consensus orb; variance decreases visually as spread shrinks.[^2_1]
- **Completion**: Consensus orb emits a single, sharp brightness pulse; message count displayed as a numeric overlay that animates from naive `n²` down to actual `messagesRequired`.[^2_1]


### 3.6 Tone.js Audio Contract

- **Base**: Low‑level, shimmering pad in the violet/quantum motif; `fm` timbre with slow LFO.[^2_1]
- **Constructive interference**: As consensus improves (KL divergence vs consensus drops), harmony becomes more consonant (intervals gravitate toward fifths/thirds).[^2_1]
- **Destructive interference**: Misalignment events produce brief dissonant clusters that quickly resolve as the system converges.[^2_1]

Mapping suggestion (non‑fabricated, high‑level only): use `entropyScore` or a derived divergence metric (once available) to drive chord tension.[^2_1]

### 3.7 React Component Interface

```ts
// src/views/pipeline/modules/QIPCView.tsx

export interface QIPCViewProps {
  state: ModuleState;              // moduleId === 'QIPC'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function QIPCView(props: QIPCViewProps) {
  return null as any;
}
```


***

Next batch will cover **TSCC, HDR, and PCFE** with the same rigor: emulation class contracts, benchmark shapes, shader/animation/audio specs, and React interfaces — still carefully avoiding any algorithmic fabrication that isn’t documented in your LOCO/NEXUS materials.

Which order do you prefer for the remaining batches: strictly in LOCO pipeline order (TSCC→HDR→PCFE next), or do you want me to interleave an exotic (e.g., DreamConsolidator) into the next trio?
<span style="display:none">[^2_10][^2_11][^2_12][^2_13][^2_14][^2_15][^2_16][^2_17][^2_18][^2_19][^2_20][^2_21][^2_22][^2_23][^2_24][^2_25][^2_26][^2_27][^2_28][^2_29][^2_30][^2_4][^2_5][^2_6][^2_7][^2_8][^2_9]</span>

<div align="center">⁂</div>

[^2_1]: SIGMA-ACTIVATION-PROMPT.md

[^2_2]: pasted-text.txt

[^2_3]: pasted-text.txt

[^2_4]: https://github.com/QwenLM/Qwen3-VL

[^2_5]: https://github.com/gstoica27/KnOTS/actions

[^2_6]: https://github.com/gstoica27/KnOTS/activity

[^2_7]: https://github.com/karpathy/nanoGPT

[^2_8]: https://github.com/lennart-finke/knottingham

[^2_9]: https://github.com/winsnip/Nexus

[^2_10]: https://github.com/microsoft/vscode-mssql/issues/1791

[^2_11]: https://gist.github.com/githubfoam/66c9b8e08498867df393a600a290de45

[^2_12]: https://github.com/Creepsky/Nexus

[^2_13]: https://github.com/langchain-ai/langgraph/issues/1248

[^2_14]: https://github.com/Lucksi/Mr.Holmes

[^2_15]: https://github.com/nexus-xyz/nexus-cli/actions/runs/16842105175/workflow

[^2_16]: https://github.com/vinzscam/backstage-chart

[^2_17]: https://github.com/mher/flower

[^2_18]: https://github.com/cloudogu/nexus-claim

[^2_19]: https://github.com/THU-KEG/KNOT/actions

[^2_20]: https://github.com/knot

[^2_21]: https://pkg.go.dev/github.com/hootuu/knot

[^2_22]: https://libraries.io/npm/clawnexus-skill

[^2_23]: https://www.sigmacomputing.com/resources/creator/sigma-created

[^2_24]: https://vuphan314.github.io/Knotty/

[^2_25]: https://libraries.io/npm/clawnexus-plugin

[^2_26]: https://libraries.io/nuget/Knotty

[^2_27]: https://github.com/gstoica27/KnOTS/releases

[^2_28]: https://apps.apple.com/pl/app/nexusclaw/id6758997411?l=pl

[^2_29]: https://iknott.github.io

[^2_30]: https://github.com/nexus-xyz/nexus-cli/actions


---

# next

Continuing in strict LOCO pipeline order, this batch covers **TSCC, HDR, and PCFE** at the same SDR depth: emulation contracts with explicit “no‑guess” boundaries, plus visual/animation/audio and React interface specs.[^3_1][^3_2][^3_3]

***

## MODULE 4 — TSCC (Temporal Sharding w/ Causal Consistency)

### 4.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a temporal sharding engine that forks timelines into parallel shards, executes them, and merges only those branches that are causally compatible; conflicts produce new forked branches instead of silent overwrites.[^3_1]
- **Inputs**:
    - `input: unknown` — module input from QIPC.
    - Optional `currentTimelineId` and `timestamp` from pipeline context.[^3_1]
- **Outputs** (per call):
    - `shards: Array<{ id: string; parentId?: string; status: 'active' | 'merged' | 'conflict'; }>`.
    - `mergeEvents: Array<{ from: string; into: string; compatible: boolean }>`.[^3_1]
    - `processingTimeMs` in `ModuleState`, plus any TSCC‑specific metrics.

**Behavioral constraints**:[^3_1]

- “Forking timeline” metaphor: from a single source, multiple parallel rails branch and later merge where causally compatible.
- Shards with causal conflicts must be marked and either isolated or re‑forked; they cannot silently merge into a mainline.
- Merge decisions and conflicts must be deterministically reproducible for a given input run.


### 4.2 TypeScript Emulation Class

```ts
// src/simulation/modules/tscc-emulator.ts

export interface TSCCShard {
  id: string;
  parentId?: string;
  status: 'active' | 'merged' | 'conflict';
}

export interface TSCCMergeEvent {
  from: string;
  into: string;
  compatible: boolean;
}

export interface TSCCOutput {
  shards: TSCCShard[];
  mergeEvents: TSCCMergeEvent[];
}

export class TSCCEmulator {
  process(input: unknown): TSCCOutput {
    // Must:
    // - Represent branching into multiple shards.
    // - Represent attempts to merge shards with a boolean compatibility flag.
    // - Never silently "fix" conflicts; mark them as status 'conflict'.
    // Exact causal compatibility rules MUST be derived from loco/src/tscc.py
    // or README; until then, this method throws NOT_FOUND_IN_REPO.
    throw new Error('NOT_FOUND_IN_REPO:TSCC_PROCESS');
  }
}
```


### 4.3 Benchmark Assertion Skeleton

```ts
// tests/modules/tscc-emulator.bench.test.ts

it('TSCC creates multiple shards and merges only compatible ones', () => {
  // Arrange: synthetic input that should create at least 2 shards.
  // Act: process().
  // Assert: at least one mergeEvents entry with compatible=true
  //         and at least one shard with status 'conflict' for conflicting case.
  throw new Error('NOT_FOUND_IN_REPO:TSCC_BENCH_SPEC');
});
```

This encodes the semantic requirement (merge + conflict) without guessing the logic.[^3_1][^3_2]

### 4.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `shaderType: 'timeline'`.[^3_1]

- **Vertex shader**:
    - Renders multiple parallel rails as polyline strips extruded in 3D, each rail corresponding to a shard.
    - Forking: vertices branch from a common root position outward over time coordinate; each shard has a slight spatial offset.[^3_1]
- **Fragment shader**:
    - Base color: timeline amber `#F59E0B` for active shards.[^3_1]
    - Merged rails: gradient from amber to a collective color (e.g., pale gold) with smooth blending.
    - Conflicts: rails where merge failed flicker with red sparks at merge points.[^3_1]


### 4.5 Animation Sequence Spec

- **Idle**: A single central rail slowly scrolling forward (time) with faint glow.[^3_1]
- **Fork**: On TSCC `MODULE_START`, one or more rails split off from the main, animated as branching lines over ~500 ms.[^3_1]
- **Merge**: Compatible shards smoothly curve back into the main rail; at the join point, a small “merge pulse” travels forward along the mainline.[^3_1]
- **Conflict**: At a conflict merge attempt, two rails collide and emit sparking particles; the conflicting shard bends away to form a divergent branch.[^3_1]


### 4.6 Tone.js Audio Contract

- **Base**: Metronomic, soft tick or pulse representing causal time (low‑level click at regular intervals).[^3_1]
- **Fork events**: Light upward arpeggio (two or three notes) indicating branching possibilities.
- **Merge events**: Short resolving chord; compatible merges are consonant, conflicts trigger a dissonant interval that decays quickly.[^3_1]


### 4.7 React Component Interface

```ts
// src/views/pipeline/modules/TSCCView.tsx

export interface TSCCViewProps {
  state: ModuleState;              // moduleId === 'TSCC'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function TSCCView(props: TSCCViewProps) {
  return null as any;
}
```


***

## MODULE 5 — HDR (Hyperdimensional Reasoning Engine)

### 5.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a hyperdimensional reasoning engine that manipulates high‑dimensional vectors via binding, bundling, and permutation, then projects them into a 3D visualization as a rotating hypercube lattice.[^3_1]
- **Inputs**:
    - One or more symbol vectors (e.g., `Float32Array` representations) or semantic placeholders for concepts.[^3_1]
- **Outputs**:
    - Emergent vector(s) representing bound, bundled, and permuted combinations.[^3_1]
    - Metrics: `latencyMs`, optional `entropyScore`, possibly `compressionRatio` if HDR participates in compression claims.[^3_1]

Behavior:[^3_1]

- Binding: pairwise operation combining two vectors into a new one (e.g., circular convolution or XOR‑style operation; exact math from README/code).
- Bundling: combining multiple bound vectors into a single composite representation.
- Permutation: re‑ordering dimensions of a vector to encode structure (e.g., sequence position).
- Visual: 10,000‑dimensional vector projected as a 3D hypercube lattice.


### 5.2 TypeScript Emulation Class

```ts
// src/simulation/modules/hdr-emulator.ts

export interface HDRInput {
  vectors: Float32Array[];  // assumed same length, high dimension
}

export interface HDROutput {
  boundVectors: Float32Array[];
  bundledVector: Float32Array | null;
}

export class HDREmulator {
  bind(a: Float32Array, b: Float32Array): Float32Array {
    // Exact binding (e.g., circular convolution or XOR) MUST follow HDR spec.
    throw new Error('NOT_FOUND_IN_REPO:HDR_BIND');
  }

  bundle(vectors: Float32Array[]): Float32Array {
    // Must create a composite vector; exact rule from HDR spec.
    throw new Error('NOT_FOUND_IN_REPO:HDR_BUNDLE');
  }

  permute(v: Float32Array, permutationId: string): Float32Array {
    // Must apply a deterministic permutation keyed by permutationId.
    throw new Error('NOT_FOUND_IN_REPO:HDR_PERMUTE');
  }

  process(input: HDRInput): HDROutput {
    // Orchestrates bind/bundle/permute operations per HDR README.
    throw new Error('NOT_FOUND_IN_REPO:HDR_PROCESS');
  }
}
```


### 5.3 Benchmark Assertion Skeleton

```ts
// tests/modules/hdr-emulator.bench.test.ts

it('HDR bundling retains approximate similarity for constituent vectors', () => {
  // Arrange: set of vectors representing related concepts.
  // Act: bundle() and compare cosine similarity vs each input.
  // Assert: expect(similarity).toBeGreaterThan(/* threshold */ 0.5);
  throw new Error('NOT_FOUND_IN_REPO:HDR_BENCH_SPEC');
});
```

Threshold to be tightened once README/code is inspected.[^3_1]

### 5.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `shaderType: 'fractal'` (or dedicated `'hyperdimensional'` in final enum if extended).[^3_1]

- **Vertex shader**:
    - Renders a lattice of points or small cubes arranged as a rotating hypercube projection.
    - Vertex positions modulated by the values of the current high‑dimensional vector projected into 3D (e.g., 3 of the dims drive XYZ displacement).[^3_1]
- **Fragment shader**:
    - Base color: HDR lattice gold `#FCD34D`.[^3_1]
    - Binding events: two subsets of lattice glow in distinct hues, spiraling together and then merging color to represent fused concept.
    - Bundling: a brief “supernova” effect where many points flash and then settle into a new stable pattern.[^3_1]


### 5.5 Animation Sequence Spec

- **Idle**: Slowly rotating hypercube; low‑frequency pulsing of node brightness to indicate latent structure.[^3_1]
- **Bind**: When `process()` binds two vectors, two differently colored subsets of nodes start rotating around each other and then blend into a single color set.[^3_1]
- **Bundle**: During bundling, the entire lattice contracts toward the center then expands; brightness peak in the middle of the animation.[^3_1]


### 5.6 Tone.js Audio Contract

- **Base**: Metallic, harmonic pad reflecting the “mathematical” nature of HDR (e.g., `fm` or `square` timbre).[^3_1]
- **Bind events**: Two notes slide toward each other in pitch and then converge on a unison when binding completes.
- **Bundle events**: Rapid cluster arpeggio that resolves into a single sustained chord.[^3_1]


### 5.7 React Component Interface

```ts
// src/views/pipeline/modules/HDRView.tsx

export interface HDRViewProps {
  state: ModuleState;              // moduleId === 'HDR'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function HDRView(props: HDRViewProps) {
  return null as any;
}
```


***

## MODULE 6 — PCFE (Predictive Coding Free Energy Engine)

### 6.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a predictive coding system that generates predictions, computes surprise / prediction error, and minimizes free energy by updating internal beliefs; visually, it should emphasize “only the deltas light up”.[^3_1]
- **Inputs**:
    - `input: unknown` — the next observation or signal.
    - Optional `priorPrediction: unknown` from a world model or previous PCFE step.[^3_1]
- **Outputs**:
    - `prediction: unknown` — predicted observation or distribution.[^3_1]
    - `errorSignal: unknown` — difference between prediction and actual.
    - `freeEnergyMetric: number` — scalar free energy / surprise measure.[^3_1]
    - Metrics: `latencyMs`, `entropyScore` (if used), etc., in `ModuleMetrics`.[^3_1]

Behavioral constraints:[^3_1]

- “Turbine of prediction waves” metaphor: smooth sine‑wave‑like predictions; only jagged deltas (surprise spikes) are visually emphasized.
- Boring, predictable content should fade to silence visually and sonically; anomalies should be highly salient.
- Predictive coding update rule must be derived from README or code; this SDR does not invent the math.


### 6.2 TypeScript Emulation Class

```ts
// src/simulation/modules/pcfe-emulator.ts

export interface PCFEOutput {
  prediction: unknown;
  errorSignal: unknown;
  freeEnergyMetric: number;
}

export class PCFEEmulator {
  process(input: unknown, priorPrediction?: unknown): PCFEOutput {
    // Must:
    // - Compute a prediction for the current input.
    // - Compute an error signal.
    // - Derive a scalar freeEnergyMetric capturing surprise.
    // Exact predictive coding / free energy math MUST follow PCFE spec,
    // so this is NOT implemented here.
    throw new Error('NOT_FOUND_IN_REPO:PCFE_PROCESS');
  }
}
```


### 6.3 Benchmark Assertion Skeleton

```ts
// tests/modules/pcfe-emulator.bench.test.ts

it('PCFE free energy decreases over iterative updates on stationary input', () => {
  // Arrange: sequence of identical or slowly-varying inputs.
  // Act: repeatedly call process(), feeding back predictions as priors.
  // Assert: freeEnergyMetric shows non-increasing trend on average.
  throw new Error('NOT_FOUND_IN_REPO:PCFE_BENCH_SPEC');
});
```

This encodes the qualitative requirement (model learns and reduces surprise) without assuming a specific formula.[^3_1]

### 6.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'compression'` or a dedicated `'prediction'` type (enum extension allowed in later MBS update).[^3_1]

- **Vertex shader**:
    - Renders a turbine or ring of waveforms (think rotating ring with sine‑wave perturbations along its rim).
    - Vertex displacement along radial axis encodes prediction amplitude; additional displacement spikes encode error magnitude.[^3_1]
- **Fragment shader**:
    - Base color: muted background waves; only error spikes use hot prediction orange `#EF4444`.[^3_1]
    - The more predictable the sequence (low free energy), the dimmer the ring; error spikes flare bright orange against dark void.[^3_1]


### 6.5 Animation Sequence Spec

- **Idle**: Slow, regular sine‑wave rotation with low amplitude; faint glow only.[^3_1]
- **Prediction with low error**: Ring spins smoothly with minimal spikes, gradually dimming as system gets confident.[^3_1]
- **Anomaly**: When `freeEnergyMetric` exceeds a threshold, the turbine jerks, emits tall orange spikes, and briefly overclocks its rotation, then relaxes as it adapts.[^3_1]


### 6.6 Tone.js Audio Contract

- **Base**: Subtle rhythmic pulse synced to global simulation time; could be a filtered noise or low drum‑like thump.[^3_1]
- **Prediction**: When error is low, the soundscape is minimal, almost silent, reflecting “boring, predictable content”.
- **Anomaly**: High error/free energy triggers distinct, sharp percussive hits or glitch bursts; intensity scales with error magnitude.[^3_1]

Mapping suggestion: error magnitude → velocity/volume of percussive sample, and freeEnergyMetric → density of events.[^3_1]

### 6.7 React Component Interface

```ts
// src/views/pipeline/modules/PCFEView.tsx

export interface PCFEViewProps {
  state: ModuleState;              // moduleId === 'PCFE'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function PCFEView(props: PCFEViewProps) {
  return null as any;
}
```


***

Next batch will cover **RCT, CIG, and MFO** with the same pattern. You just said “next” last time, so I’ll continue in LOCO order by default unless you want to inject an exotic (e.g., DreamConsolidator) into one of the upcoming trios.

<div align="center">⁂</div>

[^3_1]: SIGMA-ACTIVATION-PROMPT.md

[^3_2]: pasted-text.txt

[^3_3]: pasted-text.txt


---

# next

Continuing in LOCO order, this batch covers **RCT, CIG, and MFO** with the same SDR‑level rigor and the same non‑fabrication boundaries.[^4_1][^4_2][^4_3]

***

## MODULE 7 — RCT (Reservoir Computing Temporal Prediction)

### 7.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate an Echo State Network–style reservoir that maintains temporal memory of past inputs as a dynamically evolving pool; it supports temporal prediction by exploiting reservoir dynamics rather than explicit recurrence in the core logic.[^4_1]
- **Inputs**:
    - `timeSeries: number[] | Float32Array` — recent input sequence window.
    - Optional `currentState` representing reservoir internal state passed from previous step.[^4_1]
- **Outputs**:
    - `prediction: number | number[]` — next sample or short horizon prediction.
    - `reservoirState: Float32Array` — updated internal state.
    - Metrics in `ModuleMetrics`: `latencyMs`, possibly `entropyScore`, and reservoir‑specific stats (e.g., spectral radius proxy, state norm).[^4_1]

Behavioral constraints:[^4_1]

- Visual metaphor: a **living pool of water** where ripples from past inputs are still visible and future ripples are “ghost rendered” in pale blue.
- Reservoir “remembers” by staying perpetually disturbed; state never fully settles during active sequences.[^4_1]
- Internal update equations (input weights, recurrent weights, leak rate, etc.) must be derived from LOCO README or code; we do not invent them here.


### 7.2 TypeScript Emulation Class

```ts
// src/simulation/modules/rct-emulator.ts

export interface RCTInput {
  timeSeries: Float32Array;
  previousState?: Float32Array;
}

export interface RCTOutput {
  prediction: Float32Array;      // shape determined by spec
  reservoirState: Float32Array;
}

export class RCTEmulator {
  constructor(
    private readonly stateSize: number,   // e.g., 512, from README
  ) {}

  step(input: RCTInput): RCTOutput {
    // Must:
    // - Update reservoirState from previousState and current input.
    // - Produce a prediction from reservoirState.
    // Exact ESN dynamics MUST follow RCT spec; not invented here.
    throw new Error('NOT_FOUND_IN_REPO:RCT_STEP');
  }
}
```


### 7.3 Benchmark Assertion Skeleton

```ts
// tests/modules/rct-emulator.bench.test.ts

it('RCT reservoir state encodes temporal information (qualitative)', () => {
  // Arrange: two different temporal patterns with same final value.
  // Act: evolve reservoirState for each.
  // Assert: reservoirStateA !== reservoirStateB by cosine distance > threshold.
  throw new Error('NOT_FOUND_IN_REPO:RCT_BENCH_SPEC');
});
```

This encodes the “memory of past inputs” requirement without specifying ESN math yet.[^4_1][^4_3]

### 7.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'ecosystem'` or a specific `'reservoir'` subtype in a later enum update.[^4_1]

- **Vertex shader**:
    - Renders a circular pool surface (grid mesh) where vertex heights are driven by reservoir state (e.g., projecting state dims onto radial coordinates).[^4_1]
    - Incoming inputs cause new ripples: local height perturbations near an “impact point” whose location can encode input index or time.[^4_1]
- **Fragment shader**:
    - Base color: reservoir blue `#3B82F6`.[^4_1]
    - Past inputs: darker ripples fading slowly over time.
    - Future predictions: lighter, ghost‑blue ripples rendered slightly above the surface, maybe with additive blending.[^4_1]


### 7.5 Animation Sequence Spec

- **Idle**: Almost‑flat water with very low‑amplitude noise.[^4_1]
- **Input arrival**: Droplet impact at a given location; concentric waves propagate outward and interact with existing waves.[^4_1]
- **Prediction**: Ghost ripples appear slightly ahead of time, then either match or diverge from actual subsequent ripples, visually indicating prediction performance.[^4_1]


### 7.6 Tone.js Audio Contract

- **Base**: Subtle, watery ambience (filtered noise, low‑passed, modulated by reservoir energy).[^4_1]
- **Inputs**: Each new sample or window triggers a soft pluck or drip sound; frequency can map to input magnitude.[^4_1]
- **Prediction error**: When predictions deviate strongly, additional higher‑pitched ripples or chimes indicate mismatch.[^4_1]


### 7.7 React Component Interface

```ts
// src/views/pipeline/modules/RCTView.tsx

export interface RCTViewProps {
  state: ModuleState;              // moduleId === 'RCT'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function RCTView(props: RCTViewProps) {
  return null as any;
}
```


***

## MODULE 8 — CIG (Causal Inference Graph)

### 8.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a causal inference engine that infers a directed acyclic graph (DAG) over variables and supports visualization of interventions and the distinction between causal links and spurious correlations.[^4_1]
- **Inputs**:
    - Observational data abstraction (e.g., `records: Record<string, number>[]`) or sufficient statistics; exact input shape depends on LOCO spec.
- **Outputs**:
    - `dagNodes: string[]` — variable identifiers.
    - `dagEdges: Array<{ from: string; to: string; strength: number; type: 'causal' | 'spurious' }>`.[^4_1]
    - Metrics: `latencyMs`, `entropyScore?`, count of edges / interventions.[^4_1]

Behavior:[^4_1]

- Visual metaphor: DAG where causal arrows are **rivers** flowing from cause to effect.
- Interventions: an external “hand” can redirect a river to visualize a do‑operator; spurious correlations are shallow streams that evaporate when traced.[^4_1]
- Underlying algorithm (PC, GES, etc.) must be taken from README or code; no invented statistics here.


### 8.2 TypeScript Emulation Class

```ts
// src/simulation/modules/cig-emulator.ts

export interface CIGEdge {
  from: string;
  to: string;
  strength: number;
  type: 'causal' | 'spurious';
}

export interface CIGOutput {
  nodes: string[];
  edges: CIGEdge[];
}

export class CIGEmulator {
  inferGraph(data: unknown): CIGOutput {
    // Must:
    // - Infer nodes and directed edges forming a DAG.
    // - Label some edges as 'spurious' per CIG spec.
    // - Preserve acyclicity.
    // Exact inference method MUST follow CIG README/implementation.
    throw new Error('NOT_FOUND_IN_REPO:CIG_INFER_GRAPH');
  }

  intervene(graph: CIGOutput, intervention: { node: string; newValue: unknown }): CIGOutput {
    // Simulate do(node = newValue) per spec; no invented semantics.
    throw new Error('NOT_FOUND_IN_REPO:CIG_INTERVENE');
  }
}
```


### 8.3 Benchmark Assertion Skeleton

```ts
// tests/modules/cig-emulator.bench.test.ts

it('CIG produces an acyclic graph', () => {
  // Arrange: any synthetic dataset conforming to spec.
  // Act: inferGraph().
  // Assert: edges do not form cycles (topological sort succeeds).
  throw new Error('NOT_FOUND_IN_REPO:CIG_BENCH_SPEC');
});
```

A second test can assert that at least some edges are labeled `spurious` vs `causal` when appropriate, once spec is known.[^4_1]

### 8.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'causal'`.[^4_1]

- **Vertex shader**:
    - Renders nodes as stones or small islands in a river network, placed via force‑directed layout or layered by depth (cause→effect).[^4_1]
    - Edge segments as flowing paths (polylines) with animated flow direction.[^4_1]
- **Fragment shader**:
    - Base color: causal green `#22C55E` for strong, robust causes.[^4_1]
    - Spurious edges: pale, shallow stream color that fades quickly; low opacity and shorter line thickness.
    - Interventions: when an intervention happens, the affected river segment glows brighter and may change color temporarily to indicate manipulated flow.[^4_1]


### 8.5 Animation Sequence Spec

- **Idle**: Water flows steadily along edges from cause nodes to effect nodes.[^4_1]
- **Graph discovery**: When CIG runs, edges “fill up” with water from source to sink, as if the river network is being brought online in sequence.[^4_1]
- **Intervention**: An animated “hand” (or simpler control glyph) appears, lifts a river segment, and connects it somewhere else, with new downstream flows lighting up while old spurious connections evaporate.[^4_1]


### 8.6 Tone.js Audio Contract

- **Base**: Gentle, streaming field recording–like ambience or low‑passed noise, evoking flowing water.[^4_1]
- **Strong causal edges**: Deeper, stable drones or chords.
- **Spurious edges**: Brief, higher‑pitched ephemeral tones that fade quickly.
- **Interventions**: Short, distinctive sonic gesture (e.g., a harp glissando) as the graph changes structure.[^4_1]


### 8.7 React Component Interface

```ts
// src/views/pipeline/modules/CIGView.tsx

export interface CIGViewProps {
  state: ModuleState;              // moduleId === 'CIG'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function CIGView(props: CIGViewProps) {
  return null as any;
}
```


***

## MODULE 9 — MFO (Morphogenetic Field Orchestration)

### 9.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a reaction‑diffusion–style morphogenetic field that assigns roles to agents and organizes them into self‑organizing patterns (leaders, workers, sensors, relays, memory) according to morphogen gradients.[^4_1]
- **Inputs**:
    - `agents: Agent[]` with `morphogenConcentrations` and `role` fields.[^4_1]
- **Outputs**:
    - Updated `agents` array with possibly changed `role`, `position`, and `morphogenConcentrations`.
    - Metrics: `agentCount`, role distribution, maybe a “pattern coherence” score.[^4_1]

Behavior:[^4_1]

- Visual metaphor: Turing pattern reaction‑diffusion in a dark field; gradients bloom like living coral or embryonic tissue.
- Agents self‑organize into colored clusters by role: leaders=gold, workers=silver, sensors=green, relays=blue, memory=violet.[^4_1]
- Underlying math (reaction‑diffusion equations, diffusion radius, role assignment rule) must align with LOCO `mfp.py` (note: table says `src/mfp.py`, slight name mismatch) when inspected; not invented here.[^4_1][^4_3]


### 9.2 TypeScript Emulation Class

```ts
// src/simulation/modules/mfo-emulator.ts

export interface MFOOutput {
  agents: Agent[];
}

export class MFOEmulator {
  process(agents: Agent[]): MFOOutput {
    // Must:
    // - Update morphogenConcentrations for each agent based on neighbors.
    // - Reassign roles based on concentrations and MFO spec.
    // - Optionally adjust positions to form clusters.
    // All math MUST follow MFO reaction-diffusion spec; not invented here.
    throw new Error('NOT_FOUND_IN_REPO:MFO_PROCESS');
  }
}
```


### 9.3 Benchmark Assertion Skeleton

```ts
// tests/modules/mfo-emulator.bench.test.ts

it('MFO produces stable role clusters over iterations', () => {
  // Arrange: random initial agent positions and morphogens.
  // Act: iterate process() for N steps.
  // Assert: role distribution stabilizes (changes per step drop below threshold).
  throw new Error('NOT_FOUND_IN_REPO:MFO_BENCH_SPEC');
});
```

This encodes the “self‑organizing clusters” requirement without specifying equations.[^4_1]

### 9.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'morphogen'`.[^4_1]

- **Vertex shader**:
    - Renders a 2D or 3D field (plane) onto which agents are placed as small spheres or glyphs.^
    - Local displacement and scale of each agent glyph driven by morphogen concentrations and role.[^4_1]
- **Fragment shader**:
    - Base field: dark background with reaction‑diffusion patterns (spots, stripes) in morphogen magenta `#EC4899` for gradients.[^4_1]
    - Agents: colored per role (leaders gold, workers silver, sensors green, relays blue, memory violet) with small halos indicating local morphogen density.[^4_1]
    - Field evolves over time; gradients move and form emergent patterns.


### 9.5 Animation Sequence Spec

- **Idle**: Morphogen field slowly shifting; agents gently drifting.[^4_1]
- **Activation**: When MFO runs, morphogen gradients intensify; agents migrate into clusters, with roles flipping as morphogens cross thresholds.[^4_1]
- **Stabilization**: After several iterations, clusters become relatively stable; small fluctuations remain to keep the scene alive.[^4_1]


### 9.6 Tone.js Audio Contract

- **Base**: Dense, organic pad expressing “living tissue”; could be layered `sine`/`triangle` chords.[^4_1]
- **Cluster formation**: As clusters solidify, additional harmonics stack, thickening the sound.
- **Role differentiation**: Each role might map to a different sonic motif (leaders = bright bell overtones, workers = midrange hum, sensors = sparse high blips, relays = stereo panning, memory = long reverb tails).[^4_1]


### 9.7 React Component Interface

```ts
// src/views/pipeline/modules/MFOView.tsx

export interface MFOViewProps {
  state: ModuleState;              // moduleId === 'MFO'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function MFOView(props: MFOViewProps) {
  return null as any;
}
```


***

Next batch will cover **SCF, FSR, and ZKC**. After that, the final LOCO batch is **SEM, ITC, NEUR, SWC** (a group of four; I can treat it as 3+1 if you want to keep the “3 per prompt” rhythm strict). Do you want the next batch as **SCF+FSR+ZKC** or should I swap one of those with NEUR or SWC to better match your own mental grouping?

<div align="center">⁂</div>

[^4_1]: SIGMA-ACTIVATION-PROMPT.md

[^4_2]: pasted-text.txt

[^4_3]: pasted-text.txt


---

# next

Continuing in LOCO order, this batch covers **SCF, FSR, and ZKC**.[^5_1][^5_2][^5_3]

***

## MODULE 10 — SCF (Sheaf‑Theoretic Consensus Fusion)

### 10.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a sheaf‑theoretic fusion engine that takes heterogeneous local data (text, sensor, image, etc.), applies restriction maps, and checks global consistency (cohomology); inconsistencies manifest as “fraying threads”.[^5_1]
- **Inputs**:
    - `sections: Array<{ id: string; modality: 'text' | 'sensor' | 'image' | string; data: unknown }>` — local data patches.
- **Outputs**:
    - `fusedManifold: unknown` — coherent global structure (abstract placeholder).[^5_1]
    - `consistencyScore: number` — 0–1 measure of global consistency.
    - `inconsistentSections: string[]` — IDs where cohomology fails.[^5_1]

Behavior:[^5_1]

- Visual metaphor: heterogeneous streams (text=wavy, sensor=pulse, image=pixel grid) woven into a single manifold; cohomology failures are fraying threads that refuse to connect.
- Underlying sheaf maps, restriction/gluing rules, and cohomology check must follow LOCO spec when implemented; no invented algebra here.


### 10.2 TypeScript Emulation Class

```ts
// src/simulation/modules/scf-emulator.ts

export interface SCFSection {
  id: string;
  modality: string;
  data: unknown;
}

export interface SCFOutput {
  fusedManifold: unknown;
  consistencyScore: number;
  inconsistentSections: string[];
}

export class SCFEmulator {
  fuse(sections: SCFSection[]): SCFOutput {
    // Must:
    // - Apply restriction maps and attempt to glue sections.
    // - Compute a global consistencyScore in [0,1].
    // - Identify sections participating in cohomology failures.
    // Exact sheaf theory implementation MUST follow SCF spec.
    throw new Error('NOT_FOUND_IN_REPO:SCF_FUSE');
  }
}
```


### 10.3 Benchmark Assertion Skeleton

```ts
// tests/modules/scf-emulator.bench.test.ts

it('SCF identifies at least one inconsistent section in a conflicting set', () => {
  // Arrange: synthetic sections guaranteed to be mutually inconsistent.
  // Act: fuse().
  // Assert: inconsistentSections.length > 0 and consistencyScore < 1.
  throw new Error('NOT_FOUND_IN_REPO:SCF_BENCH_SPEC');
});
```


### 10.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'sheaf'`.[^5_1]

- **Vertex shader**:
    - Renders multiple input “threads” entering a loom and being mapped onto a 2D/3D surface.
    - Thread paths are determined by modality and section ID (e.g., layered bundles).[^5_1]
- **Fragment shader**:
    - Text streams: sinuous, wavy lines.
    - Sensor streams: pulsing bands.
    - Image streams: tiled patches.
    - Fused manifold: bright, continuous tapestry; inconsistent areas render as noisy, flickering patches or dangling fibers with low opacity.[^5_1]


### 10.5 Animation Sequence Spec

- **Idle**: Loom visible but slow, gentle motion of threads.[^5_1]
- **Fusion pass**: Threads are pulled through the loom and woven; consistent areas stabilize into a smooth cloth, inconsistent ones flutter and fail to attach.[^5_1]
- **Consistency change**: As upstream modules improve coherence, previously frayed regions gradually knit into the fabric.


### 10.6 Tone.js Audio Contract

- **Base**: Soft, weaving sound (e.g., mechanical loom clacks blended into a pad).[^5_1]
- **Consistent regions**: Harmonious, consonant chords.
- **Inconsistencies**: Brief, scratchy textures or off‑key notes that persist until resolved.[^5_1]


### 10.7 React Component Interface

```ts
// src/views/pipeline/modules/SCFView.tsx

export interface SCFViewProps {
  state: ModuleState;              // moduleId === 'SCF'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function SCFView(props: SCFViewProps) {
  return null as any;
}
```


***

## MODULE 11 — FSR (Fractal Self‑Recursive Swarms)

### 11.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a hierarchy of swarms where the same swarm structure recurs at every scale; zoom reveals nested swarms, each level pulsing independently but synchronized within its hierarchy.[^5_1]
- **Inputs**:
    - `baseAgents: Agent[]` or a derived swarm descriptor.
- **Outputs**:
    - `levels: Array<{ level: number; agents: Agent[] }>` — replicated / abstracted swarm structures at multiple scales.
    - Metrics: `agentCount`, per‑level coherence scores.[^5_1]

Behavior:[^5_1]

- Visual metaphor: a living Mandelbrot–like swarm; zoom out to see swarm, zoom in to find same structure again.
- Each level pulses independently but synchronizes at its own hierarchy level; lower levels may be phase‑shifted relative to higher ones.


### 11.2 TypeScript Emulation Class

```ts
// src/simulation/modules/fsr-emulator.ts

export interface FSRLevel {
  level: number;
  agents: Agent[];
}

export interface FSROutput {
  levels: FSRLevel[];
}

export class FSREmulator {
  constructor(private readonly maxLevels: number) {}

  process(baseAgents: Agent[]): FSROutput {
    // Must:
    // - Generate a hierarchy of swarms with self-similar structure.
    // - Preserve recognizable patterns across levels per FSR spec.
    // Exact recursion/fractal construction MUST follow FSR README/code.
    throw new Error('NOT_FOUND_IN_REPO:FSR_PROCESS');
  }
}
```


### 11.3 Benchmark Assertion Skeleton

```ts
// tests/modules/fsr-emulator.bench.test.ts

it('FSR produces self-similar patterns across swarm levels', () => {
  // Arrange: baseAgents.
  // Act: process() to get multiple levels.
  // Assert: similarity(metric(level0), metric(level1)) > threshold.
  throw new Error('NOT_FOUND_IN_REPO:FSR_BENCH_SPEC');
});
```

The “metric” could be cluster count, degree distribution, species mix, etc., from spec.[^5_1]

### 11.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'fractal'`.[^5_1]

- **Vertex shader**:
    - Renders multiple nested shells or layers of particle swarms, each scaled and rotated relative to the previous.
    - Per‑level pulsation via time‑dependent scale/offset.[^5_1]
- **Fragment shader**:
    - Base color: fractal teal `#14B8A6`.[^5_1]
    - Inner levels: higher brightness and saturation; outer levels slightly desaturated.
    - Zoom operations map user camera zoom to which level is in focus; non‑focused levels fade but remain faintly visible.


### 11.5 Animation Sequence Spec

- **Idle**: Gentle pulsing of all levels with phase offsets.[^5_1]
- **Zoom interaction**: As user zooms, a cross‑fade shifts emphasis between levels; local swarm motion is continuous across zoom transitions.[^5_1]
- **Hierarchy sync**: Occasionally, all levels align for a brief, highly synchronized pulse, conveying multi‑scale coherence.


### 11.6 Tone.js Audio Contract

- **Base**: Layered arpeggios at different octaves, each representing a swarm level.[^5_1]
- **Zoom**: As the user zooms in or out, mix levels crossfade between octaves.
- **Synchronization events**: Momentary, full‑spectrum chord where all levels align rhythmically.[^5_1]


### 11.7 React Component Interface

```ts
// src/views/pipeline/modules/FSRView.tsx

export interface FSRViewProps {
  state: ModuleState;              // moduleId === 'FSR'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function FSRView(props: FSRViewProps) {
  return null as any;
}
```


***

## MODULE 12 — ZKC (Zero‑Knowledge Consensus)

### 12.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a zero‑knowledge consensus layer where agents exchange commitments and proofs about statements without revealing underlying data, achieving agreement while preserving privacy.[^5_1]
- **Inputs**:
    - `statements: Array<{ id: string; claim: string; }>` or abstract proposition descriptors.
    - Optional per‑agent votes or evidence summaries.[^5_1]
- **Outputs**:
    - `commitments: Array<{ statementId: string; agentId: string; committed: boolean }>`.[^5_1]
    - `consensusDecisions: Array<{ statementId: string; decision: 'accept' | 'reject' | 'inconclusive' }>`.[^5_1]
    - Metrics: `consensusMessages`, `latencyMs`, count of proofs checked.[^5_1]

Behavior:[^5_1]

- Visual metaphor: a cryptographic ritual — agents exchange glowing **sealed envelopes** (Pedersen commitments) in a dark room, reach agreement without opening envelopes, and homomorphically aggregate them.[^5_1]
- Individual envelopes are never opened in visualization; only seals and aggregation operations are shown.


### 12.2 TypeScript Emulation Class

```ts
// src/simulation/modules/zkc-emulator.ts

export interface ZKCStatement {
  id: string;
  claim: string;
}

export interface ZKCCommitment {
  statementId: string;
  agentId: string;
  committed: boolean;
}

export interface ZKCDecision {
  statementId: string;
  decision: 'accept' | 'reject' | 'inconclusive';
}

export interface ZKCOutput {
  commitments: ZKCCommitment[];
  decisions: ZKCDecision[];
  consensusMessages: number;
}

export class ZKCEmulator {
  runConsensus(statements: ZKCStatement[]): ZKCOutput {
    // Must:
    // - Simulate commitment and proof exchange without revealing underlying data.
    // - Produce decisions per ZKC spec.
    // - Track consensusMessages count.
    // Exact ZK proof and commitment math MUST follow ZKC README/code.
    throw new Error('NOT_FOUND_IN_REPO:ZKC_RUN_CONSENSUS');
  }
}
```


### 12.3 Benchmark Assertion Skeleton

```ts
// tests/modules/zkc-emulator.bench.test.ts

it('ZKC reaches a decision for each statement under normal conditions', () => {
  // Arrange: set of statements and benign conditions.
  // Act: runConsensus().
  // Assert: every statementId appears in decisions with non-null decision.
  throw new Error('NOT_FOUND_IN_REPO:ZKC_BENCH_SPEC');
});
```

Additional tests can compare `consensusMessages` vs non‑ZK baseline once spec provides expectations.[^5_1]

### 12.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'cryptographic'`.[^5_1]

- **Vertex shader**:
    - Renders agents as silhouettes or nodes around a circular chamber.
    - Envelopes as small, moving spheres or capsules traveling along arcs between agents.[^5_1]
- **Fragment shader**:
    - Base chamber: ZK obsidian `#1E293B` with subtle gradients.[^5_1]
    - Envelopes: bright, saturated seals glowing in species‑agnostic colors; no contents, only outer shell and seal geometry.
    - Homomorphic aggregation: envelopes merge into a more complex geometric object at the center without ever “opening”.[^5_1]


### 12.5 Animation Sequence Spec

- **Idle**: Dark chamber with faint agent outlines.[^5_1]
- **Commitment phase**: Envelopes appear at each agent, then travel to a central altar or between agents, leaving light trails.[^5_1]
- **Aggregation**: Envelopes coalesce into a single aggregated envelope or sigil; a consensus light pulse propagates to agents when a decision is reached.[^5_1]
- **Failure/inconclusive**: Aggregation object trembles, dims, or fractures slightly if no strong consensus is formed.[^5_1]


### 12.6 Tone.js Audio Contract

- **Base**: Low, ritualistic drone, maybe in a minor key.[^5_1]
- **Commitment events**: Soft chime or bell for each envelope creation.
- **Aggregation**: Deep, resonant strike when aggregated object forms; pitch and duration indicate decision type (e.g., stable consonant chord for accept, damped or unresolved chord for inconclusive).[^5_1]


### 12.7 React Component Interface

```ts
// src/views/pipeline/modules/ZKCView.tsx

export interface ZKCViewProps {
  state: ModuleState;              // moduleId === 'ZKC'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function ZKCView(props: ZKCViewProps) {
  return null as any;
}
```


***

Remaining LOCO modules are **SEM, ITC, NEUR, and SWC**. To keep your “3 per prompt” constraint, I can do **SEM+ITC+NEUR** next, then finish with **SWC** plus a short wrap tying the Organization and Communication stacks together. If you just say “next” again, I’ll proceed with SEM/ITC/NEUR.

<div align="center">⁂</div>

[^5_1]: SIGMA-ACTIVATION-PROMPT.md

[^5_2]: pasted-text.txt

[^5_3]: pasted-text.txt


---

# finish the final 4

Finishing the LOCO set, this batch specifies **SEM, ITC, NEUR, and SWC** at the same SDR level, still avoiding any fabricated algorithm internals.[^6_1][^6_2][^6_3]

***

## MODULE 13 — SEM (Self‑Evolving Meta‑Architecture)

### 13.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a genetic/meta‑learning layer that evolves the architecture itself: candidate “chromosomes” (algorithm or parameter configurations) compete in a shadow evaluation arena, with winners hot‑swapped into the live pipeline.[^6_1]
- **Inputs**:
    - `currentConfig: unknown` — current module or stack configuration.
    - `fitnessSignals: Record<string, number>` — metrics from other modules (latency, compression, consensus quality, etc.).[^6_1]
- **Outputs**:
    - `candidateConfigs: unknown[]` — candidate architectures/params evaluated in shadow.[^6_1]
    - `selectedConfig: unknown` — winning configuration.
    - Metrics: `generationIndex`, `populationSize`, `bestFitness`, `latencyMs`.[^6_1]

Behavior:[^6_1]

- Visual metaphor: **genetic programming arena** — chromosomes as colored DNA helices competing in a colosseum; winners glow brighter over generations, and hot‑swap is shown as live module replacement without dark frames.
- Must be able to adjust parameters of downstream modules (e.g., PCFE, ITC, NEUR) via well‑typed config interfaces, but those interfaces are specified in their own modules.


### 13.2 TypeScript Emulation Class

```ts
// src/simulation/modules/sem-emulator.ts

export interface SEMFitnessSignals {
  [metricName: string]: number;
}

export interface SEMOutput {
  candidateConfigs: unknown[];
  selectedConfig: unknown;
  generationIndex: number;
  populationSize: number;
  bestFitness: number;
}

export class SEMEmulator {
  constructor(
    private readonly populationSize: number,
    private readonly mutationRate: number,
  ) {}

  evolve(currentConfig: unknown, fitness: SEMFitnessSignals): SEMOutput {
    // Must:
    // - Generate candidate configurations.
    // - Evaluate them in a shadow arena using fitness signals.
    // - Select a winning configuration.
    // Exact representation of "config" and genetic operators
    // MUST follow SEM README/code; not invented here.
    throw new Error('NOT_FOUND_IN_REPO:SEM_EVOLVE');
  }
}
```


### 13.3 Benchmark Assertion Skeleton

```ts
// tests/modules/sem-emulator.bench.test.ts

it('SEM improves bestFitness over generations on a stationary task', () => {
  // Arrange: fixed-task fitness function.
  // Act: call evolve() for several generations.
  // Assert: bestFitness_n >= bestFitness_0 on average.
  throw new Error('NOT_FOUND_IN_REPO:SEM_BENCH_SPEC');
});
```


### 13.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'genetic'`.[^6_1]

- **Vertex shader**:
    - Renders a circular arena with multiple DNA‑helix meshes orbiting or standing in “slots”.
    - Winning chromosomes move toward center; losers move outward and fade.[^6_1]
- **Fragment shader**:
    - Base arena: dark floor with faint red `#DC2626` accents.[^6_1]
    - Chromosomes: colored helices where brightness encodes fitness; new generations briefly shimmer.
    - Hot‑swap: beam of light connects winning chromosome to the live module icon.


### 13.5 Animation Sequence Spec

- **Idle**: Arena with a few chromosomes gently rotating.[^6_1]
- **Generation cycle**: When SEM runs, population appears, competes (height/brightness indicating fitness), losers dim and crumble; winner glows and shoots a beam toward target module, which subtly updates its visual appearance.[^6_1]
- **Ongoing evolution**: Generation counter displayed near SEM node, incrementing over time.


### 13.6 Tone.js Audio Contract

- **Base**: Tense, evolving pad indicating ongoing optimization.[^6_1]
- **Selection events**: Short rising swoosh plus a “lock‑in” tone when a new config is chosen.
- **Improvement**: When bestFitness improves, a higher, pleasant chime plays; regressions (if allowed) use a muted or dissonant version.[^6_1]


### 13.7 React Component Interface

```ts
// src/views/pipeline/modules/SEMView.tsx

export interface SEMViewProps {
  state: ModuleState;              // moduleId === 'SEM'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function SEMView(props: SEMViewProps) {
  return null as any;
}
```


***

## MODULE 14 — ITC (Information‑Theoretic Communication)

### 14.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate a Shannon‑style compression layer that takes messages and compresses them toward the information‑theoretic minimum, exposing compression ratio and entropy; forms part of the Communication Stack (PCFE→ITC→NEUR).[^6_1]
- **Inputs**:
    - `message: unknown` — raw communication payload.
    - Possibly `priorCodebook` or statistics from previous runs.[^6_1]
- **Outputs**:
    - `compressedSizeBits: number`.
    - `originalSizeBits: number`.
    - `compressionRatio: number` — original / compressed.[^6_1]
    - `entropyEstimate: number` — bits per symbol or equivalent.[^6_1]

Behavior:[^6_1]

- Visual metaphor: **Shannon compressor** — raw wide signal to razor‑thin spike while preserving info; entropy visualized as color temperature (red=high, blue=minimal).
- Adaptive codebook must be spec‑driven; this SDR does not choose coding scheme.


### 14.2 TypeScript Emulation Class

```ts
// src/simulation/modules/itc-emulator.ts

export interface ITCOutput {
  originalSizeBits: number;
  compressedSizeBits: number;
  compressionRatio: number;
  entropyEstimate: number;
}

export class ITCEmulator {
  compress(message: unknown): ITCOutput {
    // Must:
    // - Estimate original size in bits (per ITC spec).
    // - Compute compressed size based on an information-theoretic model.
    // - Provide entropyEstimate.
    // Exact coding scheme MUST follow ITC README/code.
    throw new Error('NOT_FOUND_IN_REPO:ITC_COMPRESS');
  }
}
```


### 14.3 Benchmark Assertion Skeleton

```ts
// tests/modules/itc-emulator.bench.test.ts

it('ITC achieves higher compression on structured vs random data', () => {
  // Arrange: one highly structured message, one random-like message.
  // Act: compress() each.
  // Assert: compressionRatio_structured > compressionRatio_random.
  throw new Error('NOT_FOUND_IN_REPO:ITC_BENCH_SPEC');
});
```


### 14.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'compression'`.[^6_1]

- **Vertex shader**:
    - Represents signal as a bar or tube that narrows along its length; early segments wide (raw) and later segments thin (compressed).[^6_1]
- **Fragment shader**:
    - Early segment color: warm red/orange (high entropy).[^6_1]
    - Later segment: “Shannon ice” blue `#BAE6FD` as redundancy is removed.[^6_1]
    - EntropyEstimate modulates color temperature and subtle pixel noise: high entropy → more speckle.


### 14.5 Animation Sequence Spec

- **Idle**: Static pipeline segment with mild shimmering.[^6_1]
- **Compression event**: The wide segment flows into the thin segment; width animates from full to narrow over ~400 ms, while color shifts from red to blue.[^6_1]
- **Communication Stack overlay**: When PCFE→ITC→NEUR is highlighted, a packet is visually funneled through PCFE (removing predictable content), then ITC (bit‑level compression), then NEUR (routing along minimal paths).[^6_1]


### 14.6 Tone.js Audio Contract

- **Base**: Quiet, high‑frequency hiss representing entropy.[^6_1]
- **Compression**: As compression ratio increases, hiss narrows to a tighter, purer tone (bandwidth shrinks).
- **Highly compressed**: Brief, bright, high‑pitched ping representing information in minimal form.[^6_1]


### 14.7 React Component Interface

```ts
// src/views/pipeline/modules/ITCView.tsx

export interface ITCViewProps {
  state: ModuleState;              // moduleId === 'ITC'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function ITCView(props: ITCViewProps) {
  return null as any;
}
```


***

## MODULE 15 — NEUR (Neuroplastic Adaptive Rewiring)

### 15.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate activity‑dependent structural plasticity: Hebbian long‑term potentiation/depression (LTP/LTD), spike‑timing–dependent plasticity (STDP), homeostasis, and pruning, producing a small‑world mycelial network over time.[^6_1]
- **Inputs**:
    - `graph: NetworkGraph` — agents and synaptic edges with weights and activity metadata.[^6_1]
    - Recent spike statistics along edges.[^6_1]
- **Outputs**:
    - Updated `NetworkGraph` with adjusted weights, possible new edges, and pruned weak links.
    - Metrics: `synapticDensity`, degree distribution stats, clustering coefficients, etc., via `ModuleMetrics`.[^6_1]

Behavior:[^6_1]

- Visual metaphor: **growing mycelial network** — new connections sprout as glowing filaments, weak ones fade and retract; Hebbian strengthening thickens filaments; small‑world topology emerges visually as clusters plus long‑range bridges.
- Low‑level update rules must be aligned with `src/erm.py` (approximate path given) and README; not guessed here.


### 15.2 TypeScript Emulation Class

```ts
// src/simulation/modules/neur-emulator.ts

export interface NEUROutput {
  networkGraph: NetworkGraph;
}

export class NEUREmulator {
  process(graph: NetworkGraph): NEUROutput {
    // Must:
    // - Update edge weights based on recent spike timing/activity (Hebbian, STDP).
    // - Add/remove edges based on pruning and growth rules.
    // - Keep network within constraints that yield small-world topology per NEUR spec.
    throw new Error('NOT_FOUND_IN_REPO:NEUR_PROCESS');
  }
}
```


### 15.3 Benchmark Assertion Skeleton

```ts
// tests/modules/neur-emulator.bench.test.ts

it('NEUR drives the network toward small-world characteristics over time', () => {
  // Arrange: initial random graph.
  // Act: iterate process() with activity patterns.
  // Assert: clustering coefficient increases, average path length remains low.
  throw new Error('NOT_FOUND_IN_REPO:NEUR_BENCH_SPEC');
});
```

Specific graph metrics follow the NEUR spec once inspected.[^6_1]

### 15.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'neural'` (distinct from NEDA by form and color).[^6_1]

- **Vertex shader**:
    - Renders agents as nodes in 3D; edges as tubes or filaments between them.
    - Node positions can be influenced by force layout and morphogen fields; edges dynamically thicken/thin based on weight.[^6_1]
- **Fragment shader**:
    - Base node color: mycelial green `#4ADE80`.[^6_1]
    - Strong synapses: bright, thick, glowing filaments; weak synapses: thin, dim lines that eventually disappear (alpha→0).[^6_1]
    - New growth: filaments sprout with a budding animation from source to target.


### 15.5 Animation Sequence Spec

- **Idle**: Slight, organic twitching in node positions; edges glow faintly.[^6_1]
- **Activity**: When spikes pass along edges, local brightening occurs, and repeated co‑activation leads to visible thickening over several frames.[^6_1]
- **Pruning**: Edges with low activity slowly fade and retract back into nodes.[^6_1]
- **Small‑world emergence**: Over the course of a run, clusters tighten and a few bright long‑range edges appear.


### 15.6 Tone.js Audio Contract

- **Base**: Network‑like, crackling or subtle percussive ticks representing synaptic chatter.[^6_1]
- **Strengthening**: As synapses strengthen, ticks along those edges become more prominent or slightly lower in pitch (more “solid”).
- **Pruning**: When edges disappear, a soft, descending blip sounds, barely audible.


### 15.7 React Component Interface

```ts
// src/views/pipeline/modules/NEURView.tsx

export interface NEURViewProps {
  state: ModuleState;              // moduleId === 'NEUR'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function NEURView(props: NEURViewProps) {
  return null as any;
}
```


***

## MODULE 16 — SWC (Swarm Chemistry w/ Species Dynamics)

### 16.1 Purpose \& Behavioral Contract

- **Purpose**: Emulate an ecosystem of agent species (Explorers, Exploiters, Relays, Sentinels) with Lotka‑Volterra–like population dynamics and behavioral rules, plus side‑channel population graphs.[^6_1]
- **Inputs**:
    - `agents: Agent[]` with `species: AgentSpecies`.
    - Current population counts and interaction parameters.[^6_1]
- **Outputs**:
    - Updated `agents` (positions, active flags).
    - Species population time series for HUD graph.[^6_1]
    - Metrics: `speciesDistribution`, `agentCount`, etc., via `ModuleMetrics`.[^6_1]

Behavior:[^6_1]

- Visual metaphor: **Lotka‑Volterra ecosystem** — four species orbit and interact:
    - Explorers (red, fast, chaotic).
    - Exploiters (gold, focused).
    - Relays (white, connective).
    - Sentinels (blue, watchful, perimeter).[^6_1]
- Sidebar population graphs pulse alongside the swarm.


### 16.2 TypeScript Emulation Class

```ts
// src/simulation/modules/swc-emulator.ts

export interface SWCSpeciesCounts {
  Explorer: number;
  Exploiter: number;
  Relay: number;
  Sentinel: number;
}

export interface SWCOutput {
  agents: Agent[];
  speciesCounts: SWCSpeciesCounts;
}

export class SWCEmulator {
  process(agents: Agent[]): SWCOutput {
    // Must:
    // - Update agent positions and states based on species behavior rules.
    // - Update speciesCounts via Lotka-Volterra-style interactions.
    // Exact equations and interaction rules MUST follow SWC spec.
    throw new Error('NOT_FOUND_IN_REPO:SWC_PROCESS');
  }
}
```


### 16.3 Benchmark Assertion Skeleton

```ts
// tests/modules/swc-emulator.bench.test.ts

it('SWC exhibits non-trivial population oscillations between species', () => {
  // Arrange: initial species mix and interaction params.
  // Act: iterate process() over many steps.
  // Assert: speciesCounts time series shows oscillatory behavior (variance > 0).
  throw new Error('NOT_FOUND_IN_REPO:SWC_BENCH_SPEC');
});
```


### 16.4 GLSL Shader Pair (Visual Identity Spec)

**Shader type**: `'ecosystem'`.[^6_1]

- **Vertex shader**:
    - Renders agents as particles moving in 2D/3D, with motion parameters dependent on species:
        - Explorers: high velocity and randomness.
        - Exploiters: cluster around resource‑like points (can be abstract).
        - Relays: tend to lie along connection paths.
        - Sentinels: form perimeter rings.[^6_1]
- **Fragment shader**:
    - Colors per species:
        - Explorers: red.
        - Exploiters: gold.
        - Relays: white.
        - Sentinels: blue.[^6_1]
    - Activity: more active agents have brighter glows and longer trails.


### 16.5 Animation Sequence Spec

- **Idle**: Species drift according to their rules, mild interactions.[^6_1]
- **Perturbation**: When simulation changes (e.g., spike or parameter change), population behaviors adjust: Explorers surge into new territory, Exploiters condense, Relays thicken, Sentinels reposition.[^6_1]
- **Population HUD**: Side panel D3 graph shows four colored curves that pulse visually in sync with species counts in the swarm.[^6_1]


### 16.6 Tone.js Audio Contract

- **Base**: Swarming, slightly chaotic but musical ambience.[^6_1]
- **Per species**:
    - Explorers: high‑pitched, sporadic blips.
    - Exploiters: steady mid‑range pulses.
    - Relays: stereo‑panning whooshes.
    - Sentinels: low drones near the edge of the stereo field.[^6_1]
- As population of each species changes, mix levels and event density of its motif adjust in real time.


### 16.7 React Component Interface

```ts
// src/views/pipeline/modules/SWCView.tsx

export interface SWCViewProps {
  state: ModuleState;              // moduleId === 'SWC'
  visualConfig: ModuleVisualConfig;
  onInspect: () => void;
}

export function SWCView(props: SWCViewProps) {
  return null as any;
}
```


***

With these four, every LOCO module now has: a TS emulation contract, benchmark assertion skeletons, unique shader/visual identity, animation spec, Tone.js mapping, and a React interface, all consistent with your activation prompt and SIGMA/MBS non‑fabrication rules.[^6_1][^6_2][^6_3]

Do you want to run the same treatment next for a batch of three NEXUS‑CLAW exotica (e.g., DreamConsolidator, CuriosityEngine, EmergenceDetector) or for SmartAgent/NeuroBus/BeliefGraph first?

<div align="center">⁂</div>

[^6_1]: SIGMA-ACTIVATION-PROMPT.md

[^6_2]: pasted-text.txt

[^6_3]: pasted-text.txt

